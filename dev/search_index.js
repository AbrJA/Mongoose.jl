var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API","text":"","title":"API Reference"},{"category":"section","location":"api/#Server-Types","page":"API","text":"","title":"Server Types"},{"category":"section","location":"api/#Lifecycle","page":"API","text":"","title":"Lifecycle"},{"category":"section","location":"api/#Routing","page":"API","text":"","title":"Routing"},{"category":"section","location":"api/#Request-and-Response","page":"API","text":"","title":"Request & Response"},{"category":"section","location":"api/#Utilities","page":"API","text":"","title":"Utilities"},{"category":"section","location":"api/#WebSocket-Types","page":"API","text":"","title":"WebSocket Types"},{"category":"section","location":"api/#Middleware","page":"API","text":"","title":"Middleware"},{"category":"section","location":"api/#Errors","page":"API","text":"","title":"Errors"},{"category":"section","location":"api/#JSON","page":"API","text":"JSON support is enabled by extending encode:\n\nusing JSON\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nThen use Response(Json, value) anywhere in your handlers.","title":"JSON"},{"category":"section","location":"api/#Static-Router","page":"API","text":"","title":"Static Router"},{"category":"type","location":"api/#Mongoose.Async","page":"API","text":"Async(router=Router(); nworkers=4, nqueue=1024, poll_timeout=0, max_body,\n           drain_timeout, request_timeout=0, errors=Dict{Int,Response}())\n\nCreate a multi-threaded server with nworkers background tasks.\n\nwarning: AOT Compilation\nAsync compiles with juliac --trim=safe but worker tasks do not receive CPU time in current AOT executables because the Julia task scheduler is not fully operational. Use Server for static binaries.\n\nKeyword Arguments\n\nnworkers::Integer: Number of worker tasks (default: 4).\nnqueue::Integer: Channel buffer size (default: 1024).\npoll_timeout::Integer: Event-loop poll timeout in ms (default: 0).\nmax_body::Integer: Maximum request body size in bytes (default: 1MB).\ndrain_timeout::Integer: Graceful shutdown drain timeout (default: 5000ms).\nrequest_timeout::Integer: Per-request timeout in ms, 0 = disabled (default: 0).\nerrors::Dict{Int,Response}: Custom responses keyed by HTTP status code (500, 413, 504). See fail!.\n\n\n\n\n\nAsync(router, config::Config)\n\nCreate an Async from a Config struct.\n\n\n\n\n\n","title":"Mongoose.Async"},{"category":"type","location":"api/#Mongoose.Server","page":"API","text":"Server(router=Router(); poll_timeout=1, max_body, drain_timeout, errors)\n\nCreate a single-threaded blocking server. Compatible with juliac --trim=safe.\n\nKeyword Arguments\n\npoll_timeout::Integer: Event-loop poll timeout in ms (default: 1). Use 0 for minimum latency at the cost of CPU.\nmax_body::Integer: Maximum request body size in bytes (default: 1MB).\ndrain_timeout::Integer: Graceful shutdown drain timeout (default: 5000ms).\nerrors::Dict{Int,Response}: Custom responses keyed by HTTP status code (500, 413, 504). See fail!.\n\n\n\n\n\nServer(router, config::Config)\n\nCreate a Server from a Config struct.\n\nnote: Note\nrequest_timeout is ignored by Server (single-threaded, no task to enforce it). Use Async for per-request timeouts.\n\n\n\n\n\n","title":"Mongoose.Server"},{"category":"function","location":"api/#Mongoose.start!","page":"API","text":"start!(server; host, port, blocking)\n\nStart the Mongoose HTTP server. Initializes the manager, binds an HTTP listener, starts worker threads (for Async), and begins the event loop.\n\nWhen blocking=true, InterruptException (Ctrl+C) is caught and triggers a graceful shutdown automatically — no wrapper code needed in the caller.\n\nKeyword Arguments\n\nhost::AbstractString: IP address to bind to (default: \"127.0.0.1\").\nport::Integer: Port number to listen on (default: 8080).\nblocking::Bool: If true, blocks until the server is stopped (default: true).\n\n\n\n\n\n","title":"Mongoose.start!"},{"category":"function","location":"api/#Mongoose.shutdown!","page":"API","text":"shutdown!() — Stop all running servers.\nCollects servers first to avoid mutating the registry during iteration.\n\n\n\n\n\nshutdown!(server)\n\nGracefully stop the server:\n\nSignal the event loop to stop.\nWait for in-flight requests to drain (up to drain_timeout).\nStop worker threads.\nFree all C resources.\nUnregister from the global registry.\n\n\n\n\n\n","title":"Mongoose.shutdown!"},{"category":"function","location":"api/#Mongoose.plug!","page":"API","text":"plug!(server, middleware; paths=nothing)\n\nRegister a middleware. Middleware is executed in FIFO order. Each middleware is a callable <: AbstractMiddleware that receives (request, params, next) and must call next() or return early.\n\nKeyword Arguments\n\npaths::Union{Nothing, Vector{String}}: If provided, the middleware only runs for requests whose URI starts with one of the given prefixes. Requests to other paths skip this middleware and go directly to next().\n\nExample\n\nplug!(server, cors())                                          # all paths\nplug!(server, apikey(keys=Set([\"k\"])); paths=[\"/api\"])         # only /api/*\nplug!(server, logger(); paths=[\"/api\", \"/admin\"])               # selective logging\n\nnote: Note\nMiddleware is only supported with the dynamic route! API. The static @router macro bypasses middleware for trim-safe compilation.\n\n\n\n\n\n","title":"Mongoose.plug!"},{"category":"function","location":"api/#Mongoose.mount!","page":"API","text":"mount!(server, directory)\n\nConfigure the server to serve static files from directory using the Mongoose C library (mg_http_serve_dir). This bypasses the Julia middleware pipeline and is handled directly on the event-loop thread for maximum performance.\n\nFeatures provided by the C library at no extra cost:\n\nRange / partial content (206) for large files and media streaming\nETag and Last-Modified / conditional 304 Not Modified\nPre-compressed .gz files served with Content-Encoding: gzip\nDirectory index (index.html) fallback\nAutomatic MIME type detection\n\nnote: Note\nStatic files are served before the middleware pipeline. If you need access control on static assets, use a reverse proxy or serve them from a separate path that is only reachable after authentication.\n\nExample\n\nmount!(server, \"public\")                       # GET /* → public/*\nmount!(server, \"public/assets\"; uri_prefix=\"/assets\")  # GET /assets/* → public/assets/*\n\n\n\n\n\n","title":"Mongoose.mount!"},{"category":"function","location":"api/#Mongoose.fail!","page":"API","text":"fail!(server, status::Int, response::Response)\n\nRegister a custom Response to be returned for the given HTTP status code. Applies to: 500 (unhandled exception), 413 (body too large), 504 (timeout).\n\nCustom 404/405 responses are better handled via a wildcard route:\n\nroute!(router, :get, \"*\", req -> Response(Html, read(\"404.html\", String); status=404))\n\nExample\n\nfail!(server, 500, Response(Json, \"\"\"{\"error\":\"Internal error\"}\"\"\"; status=500))\nfail!(server, 413, Response(Json, \"\"\"{\"error\":\"Body too large\"}\"\"\"; status=413))\n\n\n\n\n\n","title":"Mongoose.fail!"},{"category":"type","location":"api/#Mongoose.Router","page":"API","text":"Router — Trie-based dynamic HTTP + WebSocket router.\n\n\n\n\n\n","title":"Mongoose.Router"},{"category":"function","location":"api/#Mongoose.route!","page":"API","text":"route!(router, method, path, handler) -> router\n\nRegister an HTTP route on router for method and path.\n\npath supports:\n\nStatic segments, e.g. /health\nTyped params, e.g. /users/:id::Int\nUntyped params (String), e.g. /posts/:slug\n\nReturns the same router to allow chaining.\n\n\n\n\n\n","title":"Mongoose.route!"},{"category":"function","location":"api/#Mongoose.ws!","page":"API","text":"ws!(router, path; on_message, on_open=nothing, on_close=nothing)\n\nRegister a WebSocket endpoint.\n\nCallbacks\n\non_message(msg::Message) → Message | String | Vector{UInt8} | nothing — called for each frame.\non_open(req::Request) — called on upgrade with the HTTP request (optional).\non_close() — called on disconnect with no arguments (optional).\n\n\n\n\n\n","title":"Mongoose.ws!"},{"category":"type","location":"api/#Mongoose.Request","page":"API","text":"Request — Full HTTP request with owned string data.\n\ncontext is lazily allocated — starts as nothing and becomes a Dict{Symbol,Any} on first access via context!.\n\n\n\n\n\n","title":"Mongoose.Request"},{"category":"type","location":"api/#Mongoose.Response","page":"API","text":"Response — HTTP response.\n\n\n\n\n\n","title":"Mongoose.Response"},{"category":"function","location":"api/#Mongoose.context!","page":"API","text":"context!(req) → Dict{Symbol,Any}\n\nReturn the request context, creating it on first access.\n\n\n\n\n\n","title":"Mongoose.context!"},{"category":"type","location":"api/#Mongoose.Config","page":"API","text":"Config\n\nConsolidated configuration for Server and Async. Pass a Config as the second positional argument to either constructor instead of individual keyword arguments.\n\nFields\n\nField Default Description\npoll_timeout 1 Event-loop poll timeout in ms. Use 0 for min latency (high CPU).\nmax_body 1 MB Maximum request body size in bytes.\ndrain_timeout 5000 Graceful-shutdown drain period in ms.\nrequest_timeout 0 Per-request timeout in ms; 0 = disabled (Async only).\nnworkers 4 Number of worker tasks (Async only).\nnqueue 1024 Channel buffer size (Async only).\nerrors Dict() Custom Response objects keyed by HTTP status (500, 413, 504).\n\nExample\n\nconfig = Config(nworkers=8, request_timeout=15_000, max_body=5_242_880)\n\nserver = Async(router, config)\nplug!(server, health())\nstart!(server; host=\"0.0.0.0\", port=8080)\n\nConfig can be built from environment variables:\n\nconfig = Config(\n    nworkers          = parse(Int, get(ENV, \"WORKERS\", \"4\")),\n    max_body     = parse(Int, get(ENV, \"MAX_BODY\",  \"1048576\")),\n    request_timeout = parse(Int, get(ENV, \"REQ_TIMEOUT\", \"0\")),\n)\n\n\n\n\n\n","title":"Mongoose.Config"},{"category":"type","location":"api/#Mongoose.Message","page":"API","text":"Message — WebSocket message payload (text or binary).\n\n\n\n\n\n","title":"Mongoose.Message"},{"category":"function","location":"api/#Mongoose.cors","page":"API","text":"cors(; origins, methods, headers, max_age)\n\nCreate a CORS middleware.\n\nKeyword Arguments\n\norigins::String: Allowed origins (default: \"*\").\nmethods::String: Allowed HTTP methods (default: \"GET, POST, PUT, PATCH, DELETE, OPTIONS\").\nheaders::String: Allowed request headers (default: \"Content-Type, Authorization\").\nmax_age::Int: Preflight cache duration in seconds (default: 86400 = 24h).\n\nExample\n\nplug!(server, cors(origins=\"https://myapp.com\"))\n\n\n\n\n\n","title":"Mongoose.cors"},{"category":"function","location":"api/#Mongoose.ratelimit","page":"API","text":"ratelimit(; max_requests, window_seconds)\n\nCreate a rate-limiting middleware using a sharded fixed-window counter. Returns 429 Too Many Requests when the limit is exceeded.\n\nUses 16 independent shards internally to minimize lock contention.\n\nKeyword Arguments\n\nmax_requests::Int: Maximum requests allowed per window (default: 100).\nwindow_seconds::Int: Time window duration in seconds (default: 60).\n\nExample\n\nplug!(server, ratelimit(max_requests=50, window_seconds=30))\n\n\n\n\n\n","title":"Mongoose.ratelimit"},{"category":"function","location":"api/#Mongoose.bearer","page":"API","text":"bearer(validator)\n\nCreate a Bearer token authentication middleware. The validator function receives the token string and must return true if valid.\n\nExample\n\nplug!(server, bearer(token -> token == \"my-secret-token\"))\n\n\n\n\n\n","title":"Mongoose.bearer"},{"category":"function","location":"api/#Mongoose.apikey","page":"API","text":"apikey(; header_name, keys)\n\nCreate an API key authentication middleware.\n\nKeyword Arguments\n\nheader_name::String: Header to read the API key from (default: \"X-API-Key\").\nkeys::Set{String}: Set of valid API keys.\n\nExample\n\nplug!(server, apikey(keys=Set([\"key-123\"])))\n\n\n\n\n\n","title":"Mongoose.apikey"},{"category":"function","location":"api/#Mongoose.logger","page":"API","text":"logger(; threshold=0, output=stderr, structured=false)\n\nCreate a request-logging middleware.\n\nKeyword Arguments\n\nthreshold::Int: Only log requests slower than this (default: 0 = log all) ms.\noutput::IO: IO stream for log output (default: stderr).\nstructured::Bool: If true, emit one JSON object per line (default: false).\n\nExample\n\nplug!(server, logger())                         # plain text, all requests\nplug!(server, logger(threshold=100))         # only slow requests\nplug!(server, logger(structured=true))          # JSON structured logs\n\n\n\n\n\n","title":"Mongoose.logger"},{"category":"function","location":"api/#Mongoose.health","page":"API","text":"health(; health_check, ready_check, live_check)\n\nCreate a health check middleware for cloud-native deployments.\n\nKeyword Arguments\n\nhealth_check::Function: Function that returns true if the service is healthy (default: always true)\nready_check::Function: Function that returns true if the service is ready to accept traffic (default: always true)\nlive_check::Function: Function that returns true if the service is alive (default: always true)\n\nEndpoints\n\nGET /healthz: Overall health status (combines all checks)\nGET /readyz: Readiness for traffic (load balancers)\nGET /livez: Liveness check (process alive)\n\nExample\n\nplug!(server, health(\n    health_check = () -> check_database(),\n    ready_check = () -> check_dependencies(),\n    live_check = () -> true  # Process is always alive if running\n))\n\n\n\n\n\n","title":"Mongoose.health"},{"category":"function","location":"api/#Mongoose.metrics","page":"API","text":"metrics(; path=\"/metrics\")\n\nCreate a Prometheus-compatible metrics middleware.\n\nExposes an HTTP endpoint (default /metrics) in Prometheus text exposition format v0.0.4. Uses 8 internal shards keyed by thread ID to minimize lock contention under concurrent load.\n\nKeyword Arguments\n\npath::String: Scrape endpoint path (default: \"/metrics\").\n\nMetrics\n\nMetric Type Labels\nhttp_requests_total counter method, status\nhttp_request_duration_seconds histogram le (11 buckets: 5ms–10s)\n\nExample\n\nserver = Async(router; nworkers=4)\nplug!(server, health())\nplug!(server, metrics())   # exposes GET /metrics\n\nstart!(server, port=8080)\n\nPrometheus scrape_configs:\n\n- job_name: myapp\n  static_configs:\n    - targets: ['localhost:8080']\n  metrics_path: /metrics\n\n\n\n\n\n","title":"Mongoose.metrics"},{"category":"type","location":"api/#Mongoose.RouteError","page":"API","text":"RouteError — Thrown when route registration fails (e.g., parameter conflicts, invalid methods).\n\n\n\n\n\n","title":"Mongoose.RouteError"},{"category":"type","location":"api/#Mongoose.ServerError","page":"API","text":"ServerError — Thrown when server operations fail (e.g., memory allocation, initialization).\n\n\n\n\n\n","title":"Mongoose.ServerError"},{"category":"type","location":"api/#Mongoose.BindError","page":"API","text":"BindError — Thrown when the server fails to bind to the specified address/port.\n\n\n\n\n\n","title":"Mongoose.BindError"},{"category":"macro","location":"api/#Mongoose.@router","page":"API","text":"@router AppType block\n\nGenerate a static router for AppType. Routes are defined as: METHOD(\"/path\", handler)\n\nTyped parameters use :name::Type syntax (mirrors the dynamic router):\n\n@router MyApp begin\n    get(\"/\",               (req) -> Response(200, \"OK\"))\n    get(\"/users/:id::Int\", (req, id) -> Response(200, \"User $id\"))\nend\n\n\n\n\n\n","title":"Mongoose.@router"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"","title":"Examples"},{"category":"section","location":"examples/#Hello-World","page":"Examples","text":"The simplest possible Mongoose.jl server:\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> Response(Plain, \"Hello, World!\"))\n\nserver = Server(router)\nstart!(server, port=8080)","title":"Hello World"},{"category":"section","location":"examples/#REST-API-with-Path-Parameters","page":"Examples","text":"Dynamic path segments are captured with :name syntax. Add type annotations for automatic parsing:\n\nusing Mongoose\n\nrouter = Router()\n\n# String parameter (default)\nroute!(router, :get, \"/greet/:name\", (req, name) -> begin\n    Response(Plain, \"Hello, $(name)!\")\nend)\n\n# Typed integer parameter — invalid value (e.g. /users/abc) returns 404\nroute!(router, :get, \"/users/:id::Int\", (req, id) -> begin\n    Response(Json, \"\"\"{\"id\": $id, \"type\": \"$(typeof(id))\"}\"\"\")  \nend)\n\n# Float parameter\nroute!(router, :get, \"/price/:amount::Float64\", (req, amount) -> begin\n    tax = amount * 0.16\n    Response(Json, \"\"\"{\"amount\": $amount, \"tax\": $tax}\"\"\")\nend)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"REST API with Path Parameters"},{"category":"section","location":"examples/#Query-Parameters","page":"Examples","text":"req.query is a Dict{String,String} of the parsed query parameters. Use get to read values with defaults:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/search\", req -> begin\n    q    = get(req.query, \"q\", \"\")\n    page = something(tryparse(Int, get(req.query, \"page\", \"1\")), 1)\n    isempty(q) && return Response(Plain, \"Missing ?q= parameter\"; status=400)\n    Response(Json, \"{\\\"query\\\": \\\"$q\\\", \\\"page\\\": $page}\")\nend)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"Query Parameters"},{"category":"section","location":"examples/#JSON-Request-and-Response","page":"Examples","text":"JSON support requires JSON.jl. Extend encode once at the top of your app to enable Response(Json, ...) with automatic Content-Type.\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nstruct UserProfile\n    username::String\n    age::Int\n    active::Bool\nend\n\nrouter = Router()\n\n# Return a JSON response\nroute!(router, :get, \"/user/info\", req -> begin\n    Response(Json, Dict(\"username\" => \"Alice\", \"active\" => true))\nend)\n\n# Parse JSON from request body\nroute!(router, :post, \"/user/create\", req -> begin\n    data = JSON.parse(req.body)\n    name = get(data, \"username\", \"Guest\")\n    Response(Json, Dict(\"message\" => \"Hello, $name\"); status=201)\nend)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"JSON Request and Response"},{"category":"section","location":"examples/#Parse-Query-Parameters","page":"Examples","text":"For structured access, read each key from req.query with get and parse types manually:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/search\", req -> begin\n    q     = get(req.query, \"q\", \"\")\n    page  = something(tryparse(Int,  get(req.query, \"page\",  \"1\")), 1)\n    limit = tryparse(Int, get(req.query, \"limit\", \"\"))\n    Response(Plain, \"Searching '$q' page $page\")\nend)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"Parse Query Parameters"},{"category":"section","location":"examples/#WebSocket-Echo-Server","page":"Examples","text":"Register WebSocket endpoints with ws!. The on_message handler receives a Message whose .data field is either String (text frame) or Vector{UInt8} (binary frame).\n\nusing Mongoose\n\nrouter = Router()\n\nws!(router, \"/echo\",\n    on_message = (msg::Message) -> begin\n        if msg.data isa String\n            return Message(\"Echo: $(msg.data)\")\n        else\n            return Message(msg.data)  # Echo binary data back\n        end\n    end,\n    on_open  = (req::Request) -> println(\"Client connected from \", req.uri),\n    on_close = () -> println(\"Client disconnected\")\n)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"WebSocket Echo Server"},{"category":"section","location":"examples/#WebSocket-Upgrade-Rejection","page":"Examples","text":"Return false from on_open to reject the upgrade. The client receives 403 Forbidden and no WebSocket connection is established.\n\nusing Mongoose\n\nrouter = Router()\n\nws!(router, \"/secure\",\n    on_message = (msg::Message) -> Message(\"Hello, authenticated user!\"),\n    on_open    = (req::Request) -> begin\n        token = get(req.headers, \"authorization\", nothing)\n        # Reject if no token or wrong token\n        if token === nothing || token != \"Bearer secret\"\n            return false  # → client gets 403 Forbidden\n        end\n        @info \"WS authenticated\" uri=req.uri\n    end,\n    on_close   = () -> @info \"WS disconnected\"\n)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"WebSocket Upgrade Rejection"},{"category":"section","location":"examples/#Middleware-Stack","page":"Examples","text":"Middleware executes in registration order. Each middleware can inspect the request, short-circuit with a response, or pass through to the next handler.\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/api/data\", req -> begin\n    Response(Json, \"\"\"{\"status\": \"ok\"}\"\"\")\nend)\n\nserver = Async(router)\n\n# 1. Log all requests (method, URI, status, duration in ms)\nplug!(server, logger())\n\n# 2. CORS headers + OPTIONS preflight handling\nplug!(server, cors(origins=\"https://example.com\"))\n\n# 3. Rate limiting: 100 requests per 60 seconds per client IP\nplug!(server, ratelimit(max_requests=100, window_seconds=60))\n\n# 4. Bearer token authentication\nplug!(server, bearer(token -> token == \"my-secret-token\"))\n\nstart!(server, port=8080, blocking=false)","title":"Middleware Stack"},{"category":"section","location":"examples/#API-Key-Authentication","page":"Examples","text":"Protect endpoints with an API key header check:\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/internal\", req -> Response(Plain, \"Internal data\"))\n\nserver = Async(router)\nplug!(server, apikey(header_name=\"X-API-Key\", keys=Set([\"key-abc\", \"key-xyz\"])))\n\nstart!(server, port=8080, blocking=false)","title":"API Key Authentication"},{"category":"section","location":"examples/#Logger-with-Threshold","page":"Examples","text":"Only log requests that exceed a duration threshold — useful for identifying slow endpoints:\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/fast\", req -> Response(Plain, \"fast\"))\nroute!(router, :get, \"/slow\", req -> begin\n    sleep(0.1)\n    Response(Plain, \"slow\")\nend)\n\nserver = Async(router)\n\n# Only log requests taking longer than 50ms\nplug!(server, logger(threshold=50))\n\nstart!(server, port=8080, blocking=false)","title":"Logger with Threshold"},{"category":"section","location":"examples/#Serving-Static-Files","page":"Examples","text":"Serve a directory of HTML, CSS, JS, and other assets using the C-level file server (supports Range, ETag, Last-Modified, and gzip):\n\nusing Mongoose\n\nserver = Async(Router())\n\n# Serve files from \"public/\" directory\n# GET /style.css  →  public/style.css\n# GET /            →  public/index.html\nmount!(server, \"public\")\n\nstart!(server, port=8080, blocking=false)","title":"Serving Static Files"},{"category":"section","location":"examples/#Request-Context","page":"Examples","text":"Middleware can attach data to the request context via context!, which handlers can access:\n\nusing Mongoose\n\nstruct UserLookup <: Mongoose.AbstractMiddleware\n    db::Dict{String, String}\nend\n\nfunction (mw::UserLookup)(request, params, next)\n    token = get(request.headers, \"authorization\", nothing)\n    if token !== nothing\n        user = get(mw.db, replace(token, \"Bearer \" => \"\"), nothing)\n        if user !== nothing\n            context!(request)[:user] = user\n        end\n    end\n    return next()\nend\n\nrouter = Router()\nroute!(router, :get, \"/me\", req -> begin\n    user = get(context!(req), :user, \"anonymous\")\n    Response(Plain, \"Hello, $user!\")\nend)\n\nserver = Async(router)\nplug!(server, UserLookup(Dict(\"token-123\" => \"Alice\", \"token-456\" => \"Bob\")))\n\nstart!(server, port=8080, blocking=false)","title":"Request Context"},{"category":"section","location":"examples/#Async-Server-with-Multiple-Workers","page":"Examples","text":"For higher throughput, start Julia with multiple threads and configure the worker count:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/compute\", req -> begin\n    result = sum(rand(1_000_000))\n    Response(Plain, \"Computed: $result\")\nend)\n\n# 8 worker tasks processing requests concurrently\nserver = Async(router; nworkers=8)\nstart!(server, port=8080, blocking=false)\n\nStart Julia with threads: julia -t 8","title":"Async Server with Multiple Workers"},{"category":"section","location":"examples/#Static-Router-(AOT-Compilation)","page":"Examples","text":"For ahead-of-time compiled binaries with juliac --trim=safe, use the @router macro instead of Router():\n\nusing Mongoose\n\n@router MyApi begin\n    get(\"/\", req -> Response(Plain, \"Hello from AOT!\"))\n    get(\"/users/:id::Int\", (req, id) -> Response(Plain, \"User $id\"))\n    post(\"/echo\", req -> Response(Plain, req.body))\n    ws(\"/chat\", on_message = msg -> Message(\"Echo: $(msg.data)\"))\nend\n\nserver = Server(MyApi())\nstart!(server, port=8080)\n\nThis generates zero-allocation dispatch at compile time — no Dict lookups, no dynamic dispatch.","title":"Static Router (AOT Compilation)"},{"category":"section","location":"examples/#Full-Application-Example","page":"Examples","text":"A complete example combining multiple features:\n\nusing Mongoose, JSON\n\nstruct CreateUser\n    name::String\n    email::String\nend\n\nrouter = Router()\n\n# Health check\nroute!(router, :get, \"/health\", req -> Response(Plain, \"ok\"))\n\n# JSON API\nroute!(router, :get, \"/api/users/:id::Int\", (req, id) -> begin\n    Response(Json, Dict(\"id\" => id, \"name\" => \"User $id\"))\nend)\n\nroute!(router, :post, \"/api/users\", req -> begin\n    data = JSON.parse(req.body)\n    name = get(data, \"name\", \"\")\n    Response(Json, Dict(\"created\" => name); status=201)\nend)\n\n# WebSocket with idle timeout\nws!(router, \"/ws/notifications\",\n    on_message = (msg::Message) -> Message(\"\"\"{\"ack\": true}\"\"\"),\n    on_open    = (req::Request) -> @info \"WS client connected\"\n)\n\n# Server with full middleware stack\n# ws_idle_timeout: close WS connections that are idle for more than 60 seconds\nserver = Async(router; nworkers=4, ws_idle_timeout=60)\nplug!(server, logger(threshold=100))\nplug!(server, cors(origins=\"https://myapp.com\"))\nplug!(server, ratelimit(max_requests=200, window_seconds=60))\nmount!(server, \"public\")\n\nstart!(server, port=8080, blocking=false)","title":"Full Application Example"},{"category":"section","location":"examples/#Custom-Error-Responses","page":"Examples","text":"Register pre-built Response objects for specific HTTP status codes:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nrouter = Router()\n\nroute!(router, :get, \"/fail\", req -> error(\"Something broke\"))\nroute!(router, :get, \"/ok\", req -> Response(200, \"All good\"))\n\n# Custom 404: use a wildcard route\nroute!(router, :get, \"*\", req -> Response(Html, \"<h1>Not Found</h1>\"; status=404))\n\nserver = Async(router; request_timeout=5000)\n\nfail!(server, 500, Response(Json, Dict(\"error\" => \"Internal error\"); status=500))\nfail!(server, 413, Response(Json, \"\"\"{\"error\":\"Body too large\"}\"\"\"; status=413))\nfail!(server, 503, Response(Json, \"\"\"{\"error\":\"Service temporarily unavailable\"}\"\"\"; status=503))\nfail!(server, 504, Response(Json, \"\"\"{\"error\":\"Request timed out\"}\"\"\"; status=504))\n\nstart!(server, port=8080, blocking=false)","title":"Custom Error Responses"},{"category":"section","location":"examples/#Path-Scoped-Middleware","page":"Examples","text":"Apply middleware only to specific URL prefixes:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/\", req -> Response(200, \"Welcome\"))\nroute!(router, :get, \"/api/users\", req -> Response(200, \"User list\"))\nroute!(router, :get, \"/admin/dashboard\", req -> Response(200, \"Dashboard\"))\n\nserver = Async(router)\n\n# Auth only for /api and /admin routes\nplug!(server, bearer(t -> t == \"secret\"); paths=[\"/api\", \"/admin\"])\n\n# Rate limit only expensive API endpoints\nplug!(server, ratelimit(max_requests=10, window_seconds=60); paths=[\"/api\"])\n\n# Logger for everything\nplug!(server, logger(structured=true))\n\nstart!(server, port=8080, blocking=false)","title":"Path-Scoped Middleware"},{"category":"section","location":"examples/#Structured-JSON-Logging","page":"Examples","text":"Emit structured JSON log lines for machine-parsable logging:\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> Response(200, \"ok\"))\n\nserver = Async(router)\nplug!(server, logger(structured=true, output=open(\"access.log\", \"a\")))\n\nstart!(server, port=8080, blocking=false)\n\nEach log line is a JSON object:\n\n{\"method\":\"GET\",\"uri\":\"/\",\"status\":200,\"duration\":0.42,\"ts\":\"2025-01-15T10:30:00\"}","title":"Structured JSON Logging"},{"category":"section","location":"examples/#Prometheus-Metrics","page":"Examples","text":"Expose Prometheus-compatible metrics with the metrics() middleware. It automatically tracks request counts and latency histograms, and serves them at GET /metrics:\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/api/data\", req -> Response(Json, \"\"\"{\"ok\":true}\"\"\"))\n\nserver = Async(router; nworkers=4)\nplug!(server, health())\nplug!(server, metrics())          # serves GET /metrics\n\nstart!(server; host=\"0.0.0.0\", port=8080)\n\nSample output at GET /metrics:\n\n# HELP http_requests_total Total number of HTTP requests\n# TYPE http_requests_total counter\nhttp_requests_total{method=\"GET\",status=\"200\"} 42\n\n# HELP http_request_duration_seconds HTTP request latency in seconds\n# TYPE http_request_duration_seconds histogram\nhttp_request_duration_seconds_bucket{le=\"0.005\"} 38\nhttp_request_duration_seconds_bucket{le=\"0.01\"} 41\n...\nhttp_request_duration_seconds_bucket{le=\"+Inf\"} 42\nhttp_request_duration_seconds_sum 0.127\nhttp_request_duration_seconds_count 42\n\nPrometheus scrape_configs:\n\nscrape_configs:\n  - job_name: myapp\n    static_configs:\n      - targets: ['localhost:8080']\n    metrics_path: /metrics","title":"Prometheus Metrics"},{"category":"section","location":"examples/#Binary-Responses","page":"Examples","text":"Use the Binary format for raw byte responses:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/image\", req -> begin\n    data = read(\"logo.png\")\n    Response(Binary, data; status=200)\nend)\n\nserver = Async(router)\nstart!(server, port=8080, blocking=false)","title":"Binary Responses"},{"category":"section","location":"examples/#Production-Deployment","page":"Examples","text":"","title":"Production Deployment"},{"category":"section","location":"examples/#Environment-Driven-Configuration","page":"Examples","text":"Read server settings from environment variables with sensible defaults:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\n# --- Configuration from environment ---\nconst HOST    = get(ENV, \"HOST\", \"0.0.0.0\")\nconst PORT    = parse(Int, get(ENV, \"PORT\", \"8080\"))\nconst WORKERS = parse(Int, get(ENV, \"WORKERS\", string(Threads.nthreads())))\nconst MAX_BODY  = parse(Int, get(ENV, \"MAX_BODY\", \"5242880\"))  # 5 MB\nconst REQ_TIMEOUT = parse(Int, get(ENV, \"request_timeout\", \"30000\"))  # 30s\nconst LOG_LEVEL = get(ENV, \"LOG_LEVEL\", \"info\")\n\nrouter = Router()\n\nroute!(router, :get, \"/api/status\", req -> Response(Json, Dict(\n    \"status\" => \"ok\",\n    \"workers\" => WORKERS,\n    \"julia_version\" => string(VERSION)\n)))\n\nserver = Async(router;\n    nworkers=WORKERS,\n    max_body=MAX_BODY,\n    request_timeout=REQ_TIMEOUT,\n    ws_idle_timeout=60,\n    drain_timeout=10_000\n)\n\n# Middleware stack\nplug!(server, health())\nplug!(server, logger(structured=(LOG_LEVEL == \"debug\")))\nplug!(server, cors())\n\nstart!(server; host=HOST, port=PORT)\n\nLaunch with: HOST=0.0.0.0 PORT=3000 WORKERS=8 julia -t 8 --project server.jl","title":"Environment-Driven Configuration"},{"category":"section","location":"examples/#Graceful-Shutdown-with-Signal-Handling","page":"Examples","text":"Handle shutdown signals for clean container stops:\n\nusing Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> Response(200, \"Running\"))\n\nserver = Async(router; nworkers=4, drain_timeout=10_000)\nplug!(server, health())\n\nstart!(server; host=\"0.0.0.0\", port=8080, blocking=false)\n\n# Block main thread and handle signals\ntry\n    @info \"Server ready. Press Ctrl+C to stop.\"\n    while server.core.running[]\n        sleep(1)\n    end\ncatch e\n    if e isa InterruptException\n        @info \"Received shutdown signal\"\n    else\n        @error \"Unexpected error\" exception=(e, catch_backtrace())\n    end\nfinally\n    shutdown!(server)\nend","title":"Graceful Shutdown with Signal Handling"},{"category":"section","location":"examples/#Multi-Service-API-with-Route-Groups","page":"Examples","text":"Organize a larger API using separate routers merged into one server:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\n# --- User service ---\nfunction register_user_routes!(router)\n    route!(router, :get, \"/api/v1/users\", req -> begin\n        Response(Json, [Dict(\"id\" => 1, \"name\" => \"Alice\"), Dict(\"id\" => 2, \"name\" => \"Bob\")])\n    end)\n\n    route!(router, :get, \"/api/v1/users/:id::Int\", (req, id) -> begin\n        Response(Json, Dict(\"id\" => id, \"name\" => \"User $id\"))\n    end)\n\n    route!(router, :post, \"/api/v1/users\", req -> begin\n        data = JSON.parse(req.body)\n        Response(Json, Dict(\"id\" => 3, \"name\" => get(data, \"name\", \"\")); status=201)\n    end)\n\n    route!(router, :delete, \"/api/v1/users/:id::Int\", (req, id) -> begin\n        Response(204, \"\", \"\")\n    end)\nend\n\nfunction register_product_routes!(router)\n    route!(router, :get, \"/api/v1/products\", req -> begin\n        limit = tryparse(Int, get(req.query, \"limit\", \"\"))\n        n = something(limit, 10)\n        items = [Dict(\"id\" => i, \"name\" => \"Product $i\", \"price\" => i * 9.99) for i in 1:n]\n        Response(Json, items)\n    end)\n\n    route!(router, :get, \"/api/v1/products/:id::Int\", (req, id) -> begin\n        Response(Json, Dict(\"id\" => id, \"name\" => \"Product $id\", \"price\" => id * 9.99))\n    end)\nend\n\n# --- Assemble ---\nrouter = Router()\nregister_user_routes!(router)\nregister_product_routes!(router)\n\n# Catch-all 404\nroute!(router, :get, \"*\", req -> Response(Json, Dict(\"error\" => \"Not found\"); status=404))\n\nserver = Async(router; nworkers=4, request_timeout=15_000)\n\n# Public: health + CORS on everything\nplug!(server, health())\nplug!(server, cors())\nplug!(server, logger(structured=true))\n\n# Auth only on API routes\nplug!(server, bearer(t -> t == ENV[\"API_TOKEN\"]); paths=[\"/api\"])\n\n# Rate limit per-client\nplug!(server, ratelimit(max_requests=200, window_seconds=60); paths=[\"/api\"])\n\nstart!(server; host=\"0.0.0.0\", port=8080)","title":"Multi-Service API with Route Groups"},{"category":"section","location":"examples/#Request-Context-for-Auth-Pipelines","page":"Examples","text":"Use middleware to inject authenticated user data into the request context:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\n# --- Auth middleware that populates context ---\nstruct JWTAuth <: Mongoose.AbstractMiddleware\n    secret::String\nend\n\nfunction (mw::JWTAuth)(request, params, next)\n    token = get(request.headers, \"authorization\", nothing)\n    token === nothing && return Response(Json, \"\"\"{\"error\":\"Missing token\"}\"\"\"; status=401)\n\n    # Strip \"Bearer \" prefix\n    if length(token) > 7 && lowercase(token[1:7]) == \"bearer \"\n        token = token[8:end]\n    else\n        return Response(Json, \"\"\"{\"error\":\"Invalid scheme\"}\"\"\"; status=401)\n    end\n\n    # In production, decode and verify a real JWT here\n    # For this example, we simulate user lookup\n    ctx = context!(request)\n    ctx[:user_id] = 42\n    ctx[:role] = \"admin\"\n    ctx[:token] = token\n\n    return next()\nend\n\n# --- Role-based access control middleware ---\nstruct RequireRole <: Mongoose.AbstractMiddleware\n    roles::Set{String}\nend\n\nfunction (mw::RequireRole)(request, params, next)\n    ctx = context!(request)\n    role = get(ctx, :role, \"\")\n    if role ∉ mw.roles\n        return Response(Json, \"\"\"{\"error\":\"Insufficient permissions\"}\"\"\"; status=403)\n    end\n    return next()\nend\n\nrouter = Router()\n\nroute!(router, :get, \"/api/profile\", req -> begin\n    ctx = context!(req)\n    Response(Json, Dict(\"user_id\" => ctx[:user_id], \"role\" => ctx[:role]))\nend)\n\nroute!(router, :delete, \"/api/admin/users/:id::Int\", (req, id) -> begin\n    Response(Json, Dict(\"deleted\" => id))\nend)\n\nserver = Async(router; nworkers=4)\n\n# Apply auth to all /api routes\nplug!(server, JWTAuth(\"my-secret\"); paths=[\"/api\"])\n\n# Require admin role for /api/admin routes\nplug!(server, RequireRole(Set([\"admin\"])); paths=[\"/api/admin\"])\n\nstart!(server; port=8080, blocking=false)","title":"Request Context for Auth Pipelines"},{"category":"section","location":"examples/#Kubernetes-Ready-Health-Checks","page":"Examples","text":"Configure health checks that integrate with your infrastructure:\n\nusing Mongoose\n\n# Simulate external dependency checks\nconst DB_CONNECTED = Ref(true)\nconst CACHE_READY = Ref(true)\n\nrouter = Router()\nroute!(router, :get, \"/api/data\", req -> Response(Json, \"\"\"{\"ok\":true}\"\"\"))\n\nserver = Async(router; nworkers=4)\n\nplug!(server, health(\n    # Health check: all dependencies must be working\n    health_check = () -> DB_CONNECTED[] && CACHE_READY[],\n\n    # Readiness: is the service ready to accept traffic?\n    # Return false during startup or when draining\n    ready_check = () -> DB_CONNECTED[],\n\n    # Liveness: is the process responsive?\n    # Only return false if the process is deadlocked\n    live_check = () -> true\n))\n\nplug!(server, logger(structured=true))\n\nstart!(server; host=\"0.0.0.0\", port=8080)\n\nKubernetes probes configuration:\n\nlivenessProbe:\n  httpGet:\n    path: /livez\n    port: 8080\n  initialDelaySeconds: 5\n  periodSeconds: 10\nreadinessProbe:\n  httpGet:\n    path: /readyz\n    port: 8080\n  initialDelaySeconds: 3\n  periodSeconds: 5","title":"Kubernetes-Ready Health Checks"},{"category":"section","location":"examples/#File-Upload-with-Size-Validation","page":"Examples","text":"Handle file uploads with proper size limits and content type checking:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nrouter = Router()\n\nroute!(router, :post, \"/api/upload\", req -> begin\n    ct = get(req.headers, \"content-type\", \"\")\n\n    if !startswith(ct, \"application/json\")\n        return Response(Json, \"\"\"{\"error\":\"Unsupported media type\"}\"\"\"; status=415)\n    end\n\n    data = JSON.parse(req.body)\n    filename = get(data, \"filename\", \"\")\n    isempty(filename) && return Response(Json, \"\"\"{\"error\":\"Missing filename\"}\"\"\"; status=400)\n\n    Response(Json, Dict(\n        \"status\" => \"uploaded\",\n        \"filename\" => filename,\n        \"size\" => length(req.body)\n    ); status=201)\nend)\n\n# 10MB body limit for upload endpoint\nserver = Async(router; nworkers=4, max_body=10_485_760)\n\nplug!(server, logger())\nplug!(server, ratelimit(max_requests=30, window_seconds=60); paths=[\"/api/upload\"])\n\nstart!(server; port=8080, blocking=false)","title":"File Upload with Size Validation"},{"category":"section","location":"examples/#WebSocket-Chat-Room","page":"Examples","text":"A multi-client chat server using WebSocket:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/\", req -> Response(Html, \"\"\"\n    <html><body>\n    <h1>Chat</h1>\n    <div id=\"messages\"></div>\n    <input id=\"msg\" type=\"text\" /><button onclick=\"send()\">Send</button>\n    <script>\n      const ws = new WebSocket('ws://' + location.host + '/ws/chat');\n      ws.onmessage = e => {\n        const d = document.getElementById('messages');\n        d.innerHTML += '<p>' + e.data + '</p>';\n      };\n      function send() {\n        const input = document.getElementById('msg');\n        ws.send(input.value);\n        input.value = '';\n      }\n    </script>\n    </body></html>\n\"\"\"))\n\nws!(router, \"/ws/chat\",\n    on_message = (msg::Message) -> begin\n        # Echo back the message (in production, broadcast to all clients)\n        Message(\"User: $(msg.data)\")\n    end,\n    on_open = (req::Request) -> @info \"Client connected\",\n    on_close = () -> @info \"Client disconnected\"\n)\n\nserver = Async(router; workers=2)\nstart!(server; host=\"0.0.0.0\", port=8080)","title":"WebSocket Chat Room"},{"category":"section","location":"examples/#Static-API-Hybrid-Application","page":"Examples","text":"Serve a frontend SPA alongside a JSON API:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nrouter = Router()\n\n# --- JSON API ---\nroute!(router, :get, \"/api/v1/config\", req -> begin\n    Response(Json, Dict(\"version\" => \"1.0.0\", \"features\" => [\"auth\", \"search\"]))\nend)\n\nroute!(router, :get, \"/api/v1/search\", req -> begin\n    q = get(req.query, \"q\", \"\")\n    isempty(q) && return Response(Json, \"\"\"{\"error\":\"Missing query\"}\"\"\"; status=400)\n    Response(Json, Dict(\"query\" => q, \"results\" => []))\nend)\n\nserver = Async(router; nworkers=4)\n\n# Middleware: API-only auth\nplug!(server, apikey(keys=Set([ENV[\"API_KEY\"]])); paths=[\"/api\"])\n\n# CORS for API\nplug!(server, cors(origins=\"https://myapp.com\"); paths=[\"/api\"])\n\n# Structured logging\nplug!(server, logger(structured=true))\n\n# Serve frontend from public/ directory\n# Routes take priority, so /api/* is handled by Julia\n# Everything else falls through to static files\nmount!(server, \"public\")\n\nstart!(server; host=\"0.0.0.0\", port=8080)","title":"Static + API Hybrid Application"},{"category":"section","location":"examples/#Compiled-Binary-with-@router-(AOT)","page":"Examples","text":"Build a fully self-contained binary using juliac --trim=safe:\n\n# app.jl — compile with: juliac --trim=safe --output-exe myserver app.jl\nusing Mongoose\n\n@router MyAPI begin\n    get(\"/\", req -> Response(Json, \"\"\"{\"status\":\"ok\"}\"\"\"))\n    get(\"/users/:id::Int\", (req, id) -> Response(Json, \"\"\"{\"id\":$id}\"\"\"))\n    post(\"/echo\", req -> Response(req.body))\n    ws(\"/ws\", on_message = msg -> Message(\"Echo: $(msg.data)\"))\nend\n\nfunction main()\n    server = Server(MyAPI())\n    start!(server; host=\"0.0.0.0\", port=8080)\nend\n\nmain()\n\nThe @router macro generates a compile-time prefix trie with zero dynamic dispatch, making it compatible with Julia's AOT compilation. The resulting binary starts in milliseconds with no JIT warmup.","title":"Compiled Binary with @router (AOT)"},{"category":"section","location":"#Mongoose.jl","page":"Introduction","text":"Mongoose.jl is a lightweight, high-performance HTTP & WebSocket server for Julia, built on the Mongoose C library.\n\nKey features:\n\nMinimal dependencies — only Mongoose_jll and PrecompileTools\nSub-100ms time-to-first-response via PrecompileTools\nSync and async server modes with configurable worker pools\nDynamic and static (AOT-compatible) routing with typed path parameters\nAutomatic HEAD responses from GET handlers; typed param mismatch returns 404\nBuilt-in middleware: CORS, rate limiting, authentication, logging, static files\nWebSocket support on the same router — with upgrade rejection, frame limits, and idle timeout\n503 backpressure when the worker queue is full or too many concurrent timed tasks\nOptional JSON integration via JSON.jl with explicit setup by the user\nFull compatibility with juliac --trim=safe for compiled binaries","title":"Mongoose.jl"},{"category":"section","location":"#Installation","page":"Introduction","text":"] add Mongoose\n\nFor JSON support:\n\n] add JSON","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Introduction","text":"using Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/\", req -> Response(Plain, \"Hello!\"))\nroute!(router, :get, \"/users/:id::Int\", (req, id) -> Response(Plain, \"User $id\"))\n\nserver = Async(router; nworkers=4)\nstart!(server, port=8080, blocking=false)\n\n# When done\nshutdown!(server)","title":"Quick Start"},{"category":"section","location":"#Architecture","page":"Introduction","text":"Mongoose.jl uses a decoupled architecture: a Router defines the routes and handlers, while a Server manages networking, concurrency, and the event loop.\n\n                ┌─────────────────────────────────┐\n                │          Router                  │\n                │  route!(:get, \"/users/:id\", h)   │\n                │  ws!(\"/chat\", on_message=h)      │\n                └────────────┬────────────────────┘\n                             │\n                     ┌───────▼───────┐\n                     │    Server     │\n                     │  (event loop) │\n                     └───┬───┬───┬──┘\n                         │   │   │\n                         ▼   ▼   ▼       (Async only)\n                       Worker tasks","title":"Architecture"},{"category":"section","location":"#Server-Types","page":"Introduction","text":"Async — Runs the event loop on a background task and dispatches requests to a pool of worker tasks via channels. Ideal for production APIs and applications where you want non-blocking operation.\n\nserver = Async(router;\n    nworkers=4,                  # Number of worker tasks\n    nqueue=1024,                 # Channel buffer size\n    poll_timeout=0,              # Poll timeout (ms)\n    max_body=1048576,            # Max request/WS-frame body in bytes (default: 1 MB)\n    drain_timeout=5000,          # Graceful shutdown drain timeout (ms)\n    request_timeout=0,           # Per-request timeout (0 = disabled)\n    ws_idle_timeout=0,         # WebSocket idle timeout in seconds (0 = disabled)\n    errors=Dict{Int,Response}()  # Custom responses by status code\n)\n\nServer — Runs the event loop on the main thread (blocking). Suitable for simple scripts, or required for AOT compilation with juliac --trim=safe.\n\nserver = Server(router;\n    poll_timeout=1,              # Poll timeout (ms), default: 1\n    max_body=1048576,\n    drain_timeout=5000,\n    errors=Dict{Int,Response}()\n)","title":"Server Types"},{"category":"section","location":"#Config","page":"Introduction","text":"All constructor keyword arguments can be consolidated into a Config struct — useful for environment-driven configuration:\n\nconfig = Config(\n    nworkers        = parse(Int, get(ENV, \"WORKERS\", \"4\")),\n    max_body        = parse(Int, get(ENV, \"MAX_BODY\", \"1048576\")),\n    request_timeout = parse(Int, get(ENV, \"REQ_TIMEOUT\", \"0\")),\n    ws_idle_timeout = 60,\n    drain_timeout   = 10_000,\n)\n\nserver = Async(router, config)  # or Server(router, config)\n\nField Default Description\npoll_timeout 1 Poll timeout in ms (0 = min latency, high CPU)\nmax_body 1 MB Max request body in bytes; also the WebSocket frame size limit\ndrain_timeout 5000 Graceful-shutdown drain period in ms\nrequest_timeout 0 Per-request timeout in ms; 0 = disabled\nws_idle_timeout 0 WebSocket idle timeout in seconds; 0 = disabled\nnworkers 4 Worker tasks (Async only)\nnqueue 1024 Channel buffer size (Async only)\nerrors Dict() Custom Response keyed by status code","title":"Config"},{"category":"section","location":"#Lifecycle","page":"Introduction","text":"start!(server; host=\"127.0.0.1\", port=8080, blocking=true)\nshutdown!(server)  # Graceful: drains in-flight requests, stops workers, frees C resources","title":"Lifecycle"},{"category":"section","location":"#Routing","page":"Introduction","text":"","title":"Routing"},{"category":"section","location":"#Dynamic-Router","page":"Introduction","text":"Routes are registered with route! using a method symbol (or string) and a path pattern:\n\nrouter = Router()\n\n# Symbol methods\nroute!(router, :get, \"/hello\", req -> Response(Plain, \"hi\"))\nroute!(router, :post, \"/data\", req -> Response(Plain, \"ok\"))\nroute!(router, :put, \"/update\", req -> Response(Plain, \"updated\"))\nroute!(router, :patch, \"/patch\", req -> Response(Plain, \"patched\"))\nroute!(router, :delete, \"/remove\", req -> Response(Plain, \"deleted\"))\n\n# Other HTTP methods\nroute!(router, :options, \"/alt\", req -> Response(Plain, \"ok\"))\n\nGET routes automatically respond to HEAD requests (body omitted).","title":"Dynamic Router"},{"category":"section","location":"#Path-Parameters","page":"Introduction","text":"Path segments prefixed with : are captured as parameters. Optional type annotations trigger automatic parsing:\n\n# String parameter (default)\nroute!(router, :get, \"/users/:name\", (req, name) -> ...)\n\n# Typed parameters — invalid values return 404 (not 500)\nroute!(router, :get, \"/items/:id::Int\", (req, id) -> ...)    # /items/abc → 404\nroute!(router, :get, \"/price/:val::Float64\", (req, val) -> ...)","title":"Path Parameters"},{"category":"section","location":"#Convenience:-Routes-on-Servers","page":"Introduction","text":"Routes can be added directly on an already-created server:\n\nserver = Async(router)\nroute!(server, :get, \"/health\", req -> Response(Plain, \"ok\"))","title":"Convenience: Routes on Servers"},{"category":"section","location":"#Static-Router-(AOT)","page":"Introduction","text":"The @router macro generates a compile-time dispatch function with zero dynamic allocation. This is required for juliac --trim=safe:\n\n@router MyApi begin\n    get(\"/\", req -> Response(Plain, \"Hello\"))\n    get(\"/users/:id::Int\", (req, id) -> Response(Plain, \"User $id\"))\n    post(\"/data\", req -> Response(Plain, \"received\"))\n    ws(\"/chat\", on_message = msg -> Message(\"Echo: $(msg.data)\"))\nend\n\nserver = Server(MyApi())\nstart!(server, port=8080)","title":"Static Router (AOT)"},{"category":"section","location":"#Request-and-Response","page":"Introduction","text":"","title":"Request & Response"},{"category":"section","location":"#Request","page":"Introduction","text":"Handlers receive a Request:\n\nAccessor Returns Description\nreq.body String Raw request body\nget(req.headers, \"name\", nothing) String or nothing Case-insensitive header lookup\nreq.query Dict{String,String} Parsed query key/value map\ncontext!(req) Dict{Symbol,Any} Lazily-allocated context dict for middleware data","title":"Request"},{"category":"section","location":"#Response","page":"Introduction","text":"Construct responses with a format type, body, and optional keyword arguments:\n\n# Format type sets Content-Type automatically\nResponse(Plain, \"Hello!\")                    # text/plain, status 200\nResponse(Json, \"\"\"{\"ok\": true}\"\"\")          # application/json, status 200\nResponse(Html, \"<p>hi</p>\")                 # text/html, status 200\nResponse(Json, body; status=201)            # custom status\nResponse(Html, body; status=404)            # custom status\nResponse(Json, body; headers=[\"X-Custom\" => \"value\"])  # extra headers\n\n# Binary body (e.g., image or file bytes)\nResponse(Binary, read(\"image.png\"); status=200)\n\n# Raw form (when you already have a pre-formatted headers string)\nResponse(200, \"Content-Type: image/png\\r\\n\", read(\"image.png\"))\n\nThe format type determines the Content-Type header:\n\nFormat Content-Type\nPlain text/plain; charset=utf-8\nHtml text/html; charset=utf-8\nJson application/json; charset=utf-8\nXml application/xml; charset=utf-8\nCss text/css; charset=utf-8\nJs application/javascript; charset=utf-8\nBinary application/octet-stream","title":"Response"},{"category":"section","location":"#Query-String","page":"Introduction","text":"req.query is a Dict{String,String} of the parsed query parameters. Access values with get:\n\nroute!(router, :get, \"/search\", req -> begin\n    q    = get(req.query, \"q\", \"\")\n    page = something(tryparse(Int, get(req.query, \"page\", \"1\")), 1)\n    Response(Plain, \"Searching: $q page $page\")\nend)","title":"Query String"},{"category":"section","location":"#WebSocket-Support","page":"Introduction","text":"Register WebSocket endpoints with ws!:\n\nws!(router, \"/chat\",\n    on_message = (msg::Message) -> Message(\"Echo: $(msg.data)\"),\n    on_open    = (req::Request) -> begin\n        # Return false to reject the upgrade — sends 403 to the client\n        auth = get(req.headers, \"authorization\", nothing)\n        auth === nothing && return false\n        @info \"WS connected\" uri=req.uri\n    end,\n    on_close   = () -> println(\"disconnected\")\n)\n\nCallback Signature Notes\non_open (req::Request) → Any Called before upgrade. Return false to reject (sends 403). Optional.\non_message (msg::Message) → Message | String | Vector{UInt8} | nothing Called per frame. Return nothing for no reply.\non_close () → Any Called after close. Connection already gone — no arguments. Optional.\n\nProduction features:\n\nFrame size limit — WebSocket frames are subject to the same max_body limit as HTTP requests. Oversized frames close the connection.\nIdle timeout — set ws_idle_timeout (seconds) on the server to auto-close connections that send no frames within the window. Connections are checked every 5 seconds.\nUpgrade rejection — return false from on_open to refuse the WebSocket upgrade. The client receives 403 Forbidden and no WebSocket connection is established.\nPing/pong — RFC 6455 control frames are handled automatically.","title":"WebSocket Support"},{"category":"section","location":"#Middleware","page":"Introduction","text":"Middleware is added with plug! and executes in registration order. Each middleware can short-circuit the request (e.g., return a 401) or pass through to the next handler.","title":"Middleware"},{"category":"section","location":"#Path-Scoped-Middleware","page":"Introduction","text":"Apply middleware only to specific path prefixes:\n\nplug!(server, bearer(t -> t == \"secret\"); paths=[\"/api\", \"/admin\"])\nplug!(server, ratelimit(max_requests=10); paths=[\"/api/expensive\"])\n\nRequests to other paths bypass the middleware entirely.","title":"Path-Scoped Middleware"},{"category":"section","location":"#Logger","page":"Introduction","text":"Logs method, URI, status code, and duration for each request.\n\nplug!(server, logger())                         # Log all requests to stderr\nplug!(server, logger(threshold=50))           # Only log requests slower than 50ms\nplug!(server, logger(output=open(\"log.txt\",\"a\"))) # Custom output\nplug!(server, logger(structured=true))           # JSON log lines\n\nStructured mode emits one JSON object per line:\n\n{\"method\":\"GET\",\"uri\":\"/users/1\",\"status\":200,\"duration\":1.23,\"ts\":\"2025-01-15T10:30:00\"}","title":"Logger"},{"category":"section","location":"#CORS","page":"Introduction","text":"Handles OPTIONS preflight and adds CORS headers to all responses.\n\nplug!(server, cors())                                        # Allow all origins\nplug!(server, cors(origins=\"https://example.com\"))            # Specific origin\nplug!(server, cors(methods=\"GET, POST\", headers=\"Authorization\", max_age=3600))","title":"CORS"},{"category":"section","location":"#Rate-Limiting","page":"Introduction","text":"Fixed-window rate limiter keyed by client IP address.\n\nplug!(server, ratelimit())                                  # 100 req / 60s (defaults)\nplug!(server, ratelimit(max_requests=10, window_seconds=30)) # Stricter limits\n\nReturns 429 Too Many Requests with a Retry-After header when exceeded.","title":"Rate Limiting"},{"category":"section","location":"#Authentication","page":"Introduction","text":"Bearer token:\n\nplug!(server, bearer(token -> token == \"secret-123\"))\n\nReturns 401 with WWW-Authenticate: Bearer if missing or invalid scheme, 403 if the validator returns false.\n\nAPI key:\n\nplug!(server, apikey(header_name=\"X-API-Key\", keys=Set([\"key1\", \"key2\"])))\n\nReturns 401 if the header is missing or the key is not in the allowed set.","title":"Authentication"},{"category":"section","location":"#Prometheus-Metrics","page":"Introduction","text":"The metrics() middleware tracks request counts and latency histograms and exposes a Prometheus scrape endpoint:\n\nplug!(server, metrics())          # exposes GET /metrics\nplug!(server, metrics(path=\"/internal/metrics\"))  # custom path\n\nMetrics exposed:\n\nMetric Type Labels\nhttp_requests_total counter method, status\nhttp_request_duration_seconds histogram le (11 buckets: 5ms–10s)","title":"Prometheus Metrics"},{"category":"section","location":"#Static-Files","page":"Introduction","text":"Serve a directory of static assets using the C-level file server (supports Range, ETag, Last-Modified, and gzip):\n\nmount!(server, \"public\")\n\nServes index.html for directory requests. Returns 404 for missing files. Path traversal is blocked at the C level.","title":"Static Files"},{"category":"section","location":"#Error-Handling","page":"Introduction","text":"","title":"Error Handling"},{"category":"section","location":"#Custom-Error-Responses","page":"Introduction","text":"Register custom Response objects for specific HTTP status codes. The following codes are customizable: 500 (unhandled exception), 413 (body too large), 503 (worker queue full / server overloaded), 504 (request timeout).\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nrouter = Router()\nserver = Async(router)\n\n# Custom JSON 500 response\nfail!(server, 500, Response(Json, Dict(\"error\" => \"Internal server error\"); status=500))\n\n# Custom 413 response\nfail!(server, 413, Response(Json, \"\"\"{\"error\":\"Request body too large\"}\"\"\"; status=413))\n\n# Custom 503 response — returned when the worker channel is full or too many\n# concurrent timed tasks are in-flight (server overloaded)\nfail!(server, 503, Response(Json, \"\"\"{\"error\":\"Service temporarily unavailable\"}\"\"\"; status=503))\n\nYou can also pass a pre-built dict at construction time:\n\nrouter = Router()\nerrors = Dict{Int,Response}(\n    500 => Response(Json, \"\"\"{\"error\":\"Internal error\"}\"\"\"; status=500),\n    413 => Response(Json, \"\"\"{\"error\":\"Body too large\"}\"\"\"; status=413),\n    503 => Response(Json, \"\"\"{\"error\":\"Service unavailable\"}\"\"\"; status=503),\n)\nserver = Server(router; errors=errors)","title":"Custom Error Responses"},{"category":"section","location":"#Custom-404-Pages","page":"Introduction","text":"For a custom 404 response, add a wildcard catch-all route — no special API needed:\n\nroute!(router, :get, \"*\", req -> Response(Html, read(\"404.html\", String); status=404))","title":"Custom 404 Pages"},{"category":"section","location":"#Request-Timeout","page":"Introduction","text":"Set a per-request timeout (in milliseconds) to prevent slow handlers from blocking the server:\n\nserver = Async(router; request_timeout=5000)\n\nWhen a request exceeds the timeout, the server returns 504 Gateway Timeout.","title":"Request Timeout"},{"category":"section","location":"#Request-ID","page":"Introduction","text":"Every request is automatically assigned a unique monotonic ID, injected as an X-Request-Id response header. This is useful for correlating logs and debugging:\n\nHTTP/1.1 200 OK\nX-Request-Id: 42\nContent-Type: text/plain","title":"Request ID"},{"category":"section","location":"#JSON","page":"Introduction","text":"JSON support requires JSON.jl. Extend encode once at startup to enable Response(Json, ...) throughout your app:\n\nusing Mongoose, JSON\n\nMongoose.encode(::Type{Json}, body) = JSON.json(body)\n\nroute!(router, :post, \"/api\", req -> begin\n    # Parse body manually\n    data = JSON.parse(req.body)\n\n    # Send JSON response (auto Content-Type)\n    Response(Json, Dict(\"ok\" => true); status=201)\nend)","title":"JSON"},{"category":"section","location":"#Examples","page":"Introduction","text":"More comprehensive examples are available on the Examples page.","title":"Examples"},{"category":"section","location":"#API-Reference","page":"Introduction","text":"The full API documentation is available on the API page.","title":"API Reference"},{"category":"section","location":"#License","page":"Introduction","text":"Distributed under the GPL-2 License.","title":"License"}]
}
