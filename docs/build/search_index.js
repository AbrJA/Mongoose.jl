var documenterSearchIndex = {"docs":
[{"title":"API Reference","page":"API","location":"api.html#API-Reference","category":"section","text":"Public vs extension API. using Mongoose gives the application-facing surface (servers, requests, responses, middleware builders, helpers). The extension protocols below (router protocol, pipeline seam, format encoding, string utilities) are exported by Mongoose.Kernel and are reachable as Mongoose.<name> or via import Mongoose: <name> — both work without being re-exported at the top level, keeping the user namespace clean."},{"title":"App","page":"API","location":"api.html#App","category":"section","text":""},{"title":"Routing","page":"API","location":"api.html#Routing","category":"section","text":"get!, put!, and delete! are Base methods that Mongoose extends (the other verbs are Mongoose exports). All accept (server_or_router, path, handler) and the do-block form get!(server, path) do req … end."},{"title":"Request Processing","page":"API","location":"api.html#Request-Processing","category":"section","text":""},{"title":"Request & Response","page":"API","location":"api.html#Request-and-Response","category":"section","text":""},{"title":"Response Helpers","page":"API","location":"api.html#Response-Helpers","category":"section","text":""},{"title":"Request Helpers","page":"API","location":"api.html#Request-Helpers","category":"section","text":""},{"title":"URI & String Utilities","page":"API","location":"api.html#URI-and-String-Utilities","category":"section","text":""},{"title":"Middleware","page":"API","location":"api.html#Middleware","category":"section","text":""},{"title":"Server-Sent Events","page":"API","location":"api.html#Server-Sent-Events","category":"section","text":""},{"title":"WebSocket","page":"API","location":"api.html#WebSocket","category":"section","text":""},{"title":"Lifecycle","page":"API","location":"api.html#Lifecycle","category":"section","text":""},{"title":"Configuration","page":"API","location":"api.html#Configuration","category":"section","text":""},{"title":"Extensibility","page":"API","location":"api.html#Extensibility","category":"section","text":""},{"title":"Content Formats","page":"API","location":"api.html#Content-Formats","category":"section","text":""},{"title":"Module","page":"API","location":"api.html#Module","category":"section","text":""},{"title":"Errors","page":"API","location":"api.html#Errors","category":"section","text":""},{"title":"Mongoose.App","page":"API","location":"api.html#Mongoose.App","category":"type","text":"App — Mongoose.jl web application.\n\nUse `workers=0` for sync (default) or `workers=N` for async worker pool.\n\n# Constructors\n```julia\napp = App()                          # sync, dynamic router\napp = App(workers=4)                 # async, 4 workers\napp = App(workers=4, queue_size=2048) # async with larger queue\napp = App(router=my_router)          # bring-your-own router\n```\n\n# Configuration keyword arguments\n| Keyword                | Default            | Description                            |\n|------------------------|--------------------|----------------------------------------|\n| `workers`              | `0`                | Worker threads (0 = sync)              |\n| `queue_size`           | `1024`             | Max pending requests (async only)      |\n| `poll_timeout_ms`      | `1`                | Mongoose poll interval                 |\n| `max_body_bytes`       | `MAX_BODY_BYTES`   | Max request body size                  |\n| `drain_timeout_ms`     | `DRAIN_TIMEOUT_MS` | Graceful shutdown drain                |\n| `request_timeout_ms`   | `0`                | Per-request timeout (0 = disabled)     |\n| `ws_max_frame_bytes`   | `MAX_BODY_BYTES`   | Max WebSocket frame size               |\n| `ws_idle_timeout_ms`   | `0`                | WS idle timeout (0 = disabled)         |\n| `header_timeout_ms`    | `0`                | Close conns without a complete request |\n| `max_connections`      | `0`                | Max open connections (0 = unlimited)   |\n| `router`               | `Router()`         | Custom router instance                 |\n| `tls`                  | `nothing`          | `TLSConfig` for HTTPS                  |\n\n# Structure (DESIGN G4)\n- `app.config` — immutable `ServerConfig`.\n- `app.runtime` — mutable `RunState` (connections, manager, loop, TLS…).\n- `app.router`, `app.middlewares`, `app.errors`, `app.services`, … —\n  build-phase state; registration after `start!` throws `ServerError`.\n- `app.executor` — `SyncExecutor` or `AsyncExecutor` (the worker pool).\n\n\n\n\n\n"},{"title":"Mongoose.AbstractServer","page":"API","location":"api.html#Mongoose.AbstractServer","category":"type","text":"AbstractServer — the server protocol.\n\n`App` is the built-in implementation; custom server types implement the\nsame surface (or subtype `App`). Registration helpers (`route!`, `ws!`,\n`use!`, `serve!`, `onerror!`, `onstart!`, `onstop!`) and the read-side\nrouter protocol (`freeze!`, `isfrozen`, `matchroute`, `hasroute`,\n`haswsroutes`, `getwsendpoint`, `length`) all work without reaching\nthrough `server.router`.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.start!","page":"API","location":"api.html#Mongoose.Kernel.start!","category":"function","text":"start!(app; host, port, blocking, tls)\n\nStart the HTTP server. Initializes manager, binds listener, spawns workers (if async), runs event loop.\n\nWhen blocking=true (default), the caller blocks until shutdown, and a delivered InterruptException (Ctrl+C) triggers graceful shutdown (drain + onstop! hooks) before start! returns. Graceful shutdown depends on Julia delivering SIGINT as an exception to the waiting task; process managers that only send SIGTERM bypass it.\n\nExample\n\napp = App(workers=4)\nget!(app, \"/\") do req; json(Dict(\"ok\" => true)) end\nstart!(app; port=8080)\n\n\n\n\n\n"},{"title":"Mongoose.shutdown!","page":"API","location":"api.html#Mongoose.shutdown!","category":"function","text":"shutdown!(server)\n\nGracefully stop the server: drain requests, stop workers, free resources. In-flight requests and tracked background tasks (see background!, and over-budget handlers from request_timeout_ms) get one shared grace period of drain_timeout_ms ms before teardown; tasks still running after it are left alone, completed ones are dropped.\n\n\n\n\n\n"},{"title":"Mongoose.isrunning","page":"API","location":"api.html#Mongoose.isrunning","category":"function","text":"isrunning(server) → Bool\n\nWhether the server's event loop is currently running (started and not yet shut down).\n\n\n\n\n\n"},{"title":"Mongoose.url","page":"API","location":"api.html#Mongoose.url","category":"function","text":"url(server) → Union{Nothing,String}\n\nThe bound base URL (e.g. \"http://127.0.0.1:8080\") while running, nothing otherwise.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Router","page":"API","location":"api.html#Mongoose.Kernel.Router","category":"type","text":"Router — default `AbstractRouter` implementation.\n\nSupports: static paths, typed parameters (`:id::Int`), string parameters\n(`:slug`), and a catch-all wildcard (`*path`, must be the last segment).\n`freeze!` closes the route table (registration throws afterwards) and\ncompiles the closed table into a `CompiledDispatch` — the contract that\nmakes the router amenable to AOT/`--trim=safe` builds.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.route!","page":"API","location":"api.html#Mongoose.Kernel.route!","category":"function","text":"route!(router, method, path, handler; middleware=nothing, metadata=nothing) → router\n\nRegister an HTTP route. Supports:\n\nStatic: /health\nTyped params: /users/:id::Int\nString params: /posts/:slug\nWildcard: /*path (must be last segment)\n\nmiddleware is scoped to this route (composed with app-global middleware at dispatch time); it accepts nothing, a single middleware/callable, or a vector/tuple of them. metadata is opaque and available for future OpenAPI-style tooling.\n\nOverlapping parametric routes resolve first-registered-first at dispatch; static routes always take precedence over parametric ones.\n\n\n\n\n\nroute!(group, method, path, handler)\n\nAdd a route to a group. Path is relative to the group's prefix.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.ws!","page":"API","location":"api.html#Mongoose.Kernel.ws!","category":"function","text":"ws!(group, path; kwargs...)\nws!(group, path, handler; kwargs...)\n\nAdd a WebSocket route to a group. The handler may be positional (shorthand for on_message=handler) or passed as the on_message keyword.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.freeze!","page":"API","location":"api.html#Mongoose.Kernel.freeze!","category":"function","text":"freeze!(server) → server\n\nClose and compile the server's route table: later route!/ws! throw RouteError. See freeze! on routers for the compiled-dispatch details.\n\n\n\n\n\nfreeze!(router) → router\n\nClose the route table: any later route!/ws! throws RouteError. Dispatch keeps working. This is the closed-route contract required by AOT/--trim=safe profiles (a static route table can be compiled once and pruned).\n\nFreezing also compiles the closed table into a CompiledDispatch (see compiled.jl): each fixed route and parametric route gets a pre-baked terminal (handler + scoped middleware fused, with the handler's concrete type captured), and parametric matching runs through a statically-typed chain with no per-request path splitting. The pipeline uses the compiled path via getterminal when compiled !== nothing; matchroute keeps its generic (correct) implementation.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.isfrozen","page":"API","location":"api.html#Mongoose.Kernel.isfrozen","category":"function","text":"isfrozen(server) → Bool\n\nWhether the server's route table has been closed by freeze!.\n\n\n\n\n\nisfrozen(router) → Bool\n\nWhether freeze! has been called on this router (registration closed; the table compiled). Custom AbstractRouters default to false.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.RouteGroup","page":"API","location":"api.html#Mongoose.Kernel.RouteGroup","category":"type","text":"RouteGroup — A collection of routes sharing a prefix and middleware stack.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.group","page":"API","location":"api.html#Mongoose.Kernel.group","category":"function","text":"group(prefix; middleware=[]) → RouteGroup\n\nCreate a new route group with the given path prefix.\n\nExample\n\napi = group(\"/api/v1\", middleware=[bearer(validate_token)]) do g\n    route!(g, :get, \"/users\", list_users)\n    route!(g, :post, \"/users\", create_user)\n\n    group!(g, \"/admin\", middleware=[require_role(\"admin\")]) do admin\n        route!(admin, :delete, \"/users/:id::Int\", delete_user)\n    end\nend\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.group!","page":"API","location":"api.html#Mongoose.Kernel.group!","category":"function","text":"group!(parent, prefix; middleware=[]) do g ... end\n\nAdd a nested group to a parent group.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.mount!","page":"API","location":"api.html#Mongoose.Kernel.mount!","category":"function","text":"mount!(router_or_app, group)\n\nMount a RouteGroup into a router or app, registering all its routes with the group's prefix applied and its middleware attached as scoped route metadata. The runtime composes scoped middleware with app-global middleware at dispatch time — no per-route closures are created.\n\nExample\n\napi = group(\"/api/v1\") do g\n    get!(g, \"/users\", list_users)\n    post!(g, \"/users\", create_user)\nend\nmount!(app, api)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.matchroute","page":"API","location":"api.html#Mongoose.Kernel.matchroute","category":"function","text":"matchroute(server, method, path) → RouteResult\n\nResolve a request against the server's router, without running handlers. See matchroute.\n\n\n\n\n\nmatchroute(router, method, path) → RouteResult\n\nResolve a request to its exhaustive outcome: Matched(endpoint, handlers, params) when the route serves the method, NoMatch when the path matches nothing, or MethodMismatch{allowed} carrying the route's method bitmask. Exact (static) matches win; parametric routes are scanned in registration order; the \"*\" catch-all is the final fallback. HEAD is served only by an explicit head! route — there is no auto-HEAD fallback.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.hasroute","page":"API","location":"api.html#Mongoose.Kernel.hasroute","category":"function","text":"hasroute(server, path) → Bool\n\nWhether the server's router owns path (catch-all excluded). See hasroute.\n\n\n\n\n\nhasroute(router, path) → Bool\n\ntrue when a concrete (non-catch-all) route owns the path, regardless of method — used by static file serving to avoid shadowing registered routes (the previous match_route_exact semantic).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.gethandler","page":"API","location":"api.html#Mongoose.Kernel.gethandler","category":"function","text":"gethandler(mm, method) → Union{Nothing, Function}\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.getendpoint","page":"API","location":"api.html#Mongoose.Kernel.getendpoint","category":"function","text":"getendpoint(mm, method) → Union{Nothing, Endpoint}\n\nThe endpoint registered for method, or nothing; throws RouteError for a method outside the seven supported HTTP methods.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.haswsroutes","page":"API","location":"api.html#Mongoose.Kernel.haswsroutes","category":"function","text":"haswsroutes(server) → Bool\n\nWhether the server serves any WebSocket endpoints. See haswsroutes.\n\n\n\n\n\nhaswsroutes(router) → Bool\n\nWhether the router serves any WebSocket endpoints. Defaults to false; the default Router answers true once a ws! route is registered.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.getwsendpoint","page":"API","location":"api.html#Mongoose.Kernel.getwsendpoint","category":"function","text":"getwsendpoint(server, uri) → Union{Nothing,WSEndpoint}\n\nResolve the WebSocket endpoint for uri, or nothing. See getwsendpoint.\n\n\n\n\n\ngetwsendpoint(router, uri) → Union{Nothing,WSEndpoint}\n\nResolve the WebSocket endpoint registered for uri, or nothing when the router serves no matching WS route. Defaults to nothing.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Matched","page":"API","location":"api.html#Mongoose.Kernel.Matched","category":"type","text":"Matched{endpoint,handlers,params} <: RouteResult\n\nA successful route match. endpoint is the Endpoint for the requested method, handlers carries the route's method information (a MethodMap, or SingleEndpoint for routers with one handler), and params is the typed parameter tuple.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.NoMatch","page":"API","location":"api.html#Mongoose.Kernel.NoMatch","category":"type","text":"No route matched the path.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.MethodMismatch","page":"API","location":"api.html#Mongoose.Kernel.MethodMismatch","category":"type","text":"MethodMismatch{allowed::UInt8} <: RouteResult\n\nThe path matched but the method isn't registered; allowed is a bitmask of served methods (HEAD is implied by GET). Serialize with allow_from_bitmask for the RFC 9110 §15.5.6 Allow header.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.RouteResult","page":"API","location":"api.html#Mongoose.Kernel.RouteResult","category":"type","text":"RouteResult — outcome of `matchroute`: `Matched`, `NoMatch`, or\n`MethodMismatch{allowed}` (the last carries the route's method bitmask,\nso 405 `Allow` needs no secondary lookup).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.MethodMap","page":"API","location":"api.html#Mongoose.Kernel.MethodMap","category":"type","text":"MethodMap — Fixed-slot storage for HTTP method → endpoint mapping.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.post!","page":"API","location":"api.html#Mongoose.Kernel.post!","category":"function","text":"post!(router_or_app, path, handler)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.patch!","page":"API","location":"api.html#Mongoose.Kernel.patch!","category":"function","text":"patch!(router_or_app, path, handler)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.options!","page":"API","location":"api.html#Mongoose.Kernel.options!","category":"function","text":"options!(router_or_app, path, handler)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.head!","page":"API","location":"api.html#Mongoose.Kernel.head!","category":"function","text":"head!(router_or_app, path, handler)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.process","page":"API","location":"api.html#Mongoose.Kernel.process","category":"function","text":"process(ctx::RequestContext, request) → Response\n\nRun the full pipeline: attach services to the request context, dispatch the request through any middleware then the router, apply custom error responses for 4xx/5xx results, and map thrown exceptions (typed onerror! handlers first, then the built-in HTTPError/ValidationError mapping).\n\nArguments\n\nctx::RequestContext — router, middleware stack, error pages, DI services, and typed exception handlers (see RequestContext).\nrequest::Request — the transport-agnostic request.\n\nMiddleware is composed with the matched route's scoped Endpoint middleware (global → group/route → handler), and it wraps every terminal — including the 404/405 producers — so interception middleware (CORS, health, metrics) observes all requests.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.RequestContext","page":"API","location":"api.html#Mongoose.Kernel.RequestContext","category":"type","text":"RequestContext{R,M,E,S,H} — the app-level request-processing bundle.\n\nCollapses the config that `process` needs into one object so the\npipeline seam has a single argument: the router, the app-global middleware\nstack, the status→error-page map, the DI services, and the typed exception\nhandlers.\n\nThe global middleware stack is stored as a **baked tuple snapshot** (built\nonce by `App`/`use!` and immutable afterward), so the per-request pipeline\nnever re-grows or re-walks a mutable vector and needs no\n`[global; scoped]` concatenation.\n\nStandalone use (no server):\n```julia\nctx = RequestContext(router; errors=errs, services=(db=pool,))\nresp = process(ctx, Request(:get, \"/\", Dict{String,String}(), Pair{String,String}[], \"\"))\n```\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.getterminal","page":"API","location":"api.html#Mongoose.Kernel.getterminal","category":"function","text":"getterminal(router, request) → Union{Nothing,Function}\n\nOptional compiled-dispatch capability. A frozen Router that compiled its route table returns a pre-built terminal (req) → Response for the request (scoped middleware already fused, handler call statically typed); nothing means \"fall back to the generic dispatch path\".\n\nThis is the contract-by-fallback seam that lets the pipeline skip the per-request closure/concat allocation when the router is compiled, without forcing every router implementation to understand compilation.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.invokeendpoint","page":"API","location":"api.html#Mongoose.Kernel.invokeendpoint","category":"function","text":"invokeendpoint(endpoint, request, params) → Response\n\nCall the matched endpoint's handler with the route parameters. The built-in Endpoint dispatches handler(req, params...) (or handler(req) when there are none) — that method lives in process.jl, after Endpoint is defined.\n\nCustom routers that carry their own endpoint type implement this method (and scopedmiddleware) instead of forcing their endpoints into Mongoose.Endpoint.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.scopedmiddleware","page":"API","location":"api.html#Mongoose.Kernel.scopedmiddleware","category":"function","text":"scopedmiddleware(endpoint) → Tuple\n\nScoped middleware owned by a matched endpoint (empty tuple for custom endpoint types). Paired with invokeendpoint for custom routers; the built-in Endpoint stores a captured tuple.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.errorresponse","page":"API","location":"api.html#Mongoose.Kernel.errorresponse","category":"function","text":"errorresponse(errors, status) → Response\n\nLook up a custom error response, falling back to module defaults. errors maps HTTP status codes to a static Response or Function(req).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.runpipeline","page":"API","location":"api.html#Mongoose.Kernel.runpipeline","category":"function","text":"runpipeline(middlewares, request, handler) → Response\nrunpipeline(globals, scoped, request, handler) → Response\n\nRun the middleware onion around handler: each middleware receives (request, next); next advances to the following middleware and finally the handler. Middleware may short-circuit by returning without calling next.\n\nThe tuple form (the compiled/production path, where route-scoped middleware is already fused into the terminal) walks the baked stack with an immutable callable continuation — Next is a Function, so the (req, next) contract is unchanged, but no closure or cursor is allocated per request.\n\nThe four-argument form (generic/dev path) walks the baked app-global stack and the route-scoped stack with one cursor over their virtual concatenation — no [global; scoped] array built per request.\n\nhandler may be any 0-arity callable (Function or functor) — the compiled dispatch path passes pre-baked terminal functors.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.asmiddleware","page":"API","location":"api.html#Mongoose.Kernel.asmiddleware","category":"function","text":"asmiddleware(mw) → AbstractMiddleware\n\nNormalize any middleware into an AbstractMiddleware: AbstractMiddleware instances pass through; any other callable f(req, next) is wrapped in a FunctionMiddleware. This is the single admission point used by use!, by route!/group middleware= metadata, and by Endpoints.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.asmiddlewares","page":"API","location":"api.html#Mongoose.Kernel.asmiddlewares","category":"function","text":"asmiddlewares(input) → Vector{AbstractMiddleware}\n\nNormalize middleware input into a Vector{AbstractMiddleware}: nothing → empty, a single middleware (or plain callable) → one entry, a vector/tuple → one entry per element. Each element goes through asmiddleware.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.attach!","page":"API","location":"api.html#Mongoose.Kernel.attach!","category":"function","text":"attach!(middleware, server) → middleware\n\nOptional lifecycle hook: use! calls it when middleware is registered, so middleware that needs server state (metrics gauges, readiness checks) can capture a reference. Default is a no-op.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.AbstractRequest","page":"API","location":"api.html#Mongoose.Kernel.AbstractRequest","category":"type","text":"AbstractRequest — protocol for transport-agnostic requests.\n\n`Request` is the default implementation. Transport adapters construct\ntheir own request objects (or reuse `Request`) and the protocol layer\nnever crosses into FFI types.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Request","page":"API","location":"api.html#Mongoose.Kernel.Request","category":"type","text":"Request — Full HTTP request with owned data.\n\n`query` is parsed lazily: the transport stores the raw query string and\n[`parsequery`](@ref) memoizes the parsed `Dict` on first access, so requests\nthat never read the query pay only the raw-string copy (nothing at all when\nthere is no query). `context` is allocated on first access the same way.\n\n`services` is the app's DI NamedTuple, set by `process` before dispatch (no\ndict allocation, no boxing); [`service`](@ref)/[`withservices`](@ref) read\nit directly.\n\n`remote_addr` is the transport-provided peer address (the client's IP as a\nstring, or `nothing` when the transport does not supply one — e.g. the\nstandalone pipeline or `FakeTransport`-constructed requests).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Response","page":"API","location":"api.html#Mongoose.Kernel.Response","category":"type","text":"Response — Immutable HTTP response with structured headers.\n\nHeaders are stored as a `Vector{Pair{String,String}}` and serialized\nto wire format only when sent. Middleware can inspect and modify headers\nbefore they reach the client.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.StreamResponse","page":"API","location":"api.html#Mongoose.Kernel.StreamResponse","category":"type","text":"StreamResponse — Response whose body is produced incrementally.\n\nThe `producer` function receives a `StreamWriter` and writes chunks to it.\nThe producer runs on its own task, feeding a bounded channel that the\nevent loop drains — a slow producer never blocks the poll thread or other\nconnections. Works in both sync and async modes.\n\n# Example\n```julia\nfunction sse_handler(req)\n    StreamResponse(200, \"text/event-stream\") do writer\n        for i in 1:10\n            write(writer, \"data: event $i\\n\\n\")\n            flush(writer)\n            sleep(0.5)\n        end\n    end\nend\n```\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Headers","page":"API","location":"api.html#Mongoose.Kernel.Headers","category":"type","text":"Headers — lightweight wrapper around the parsed header pairs.\n\nOwnership of the type by Mongoose allows defining `get`/`haskey`\nwithout committing Base piracy on `Vector{Pair{String,String}}`.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.asheaders","page":"API","location":"api.html#Mongoose.Kernel.asheaders","category":"function","text":"asheaders(input) → Headers\n\nNormalize accepted header inputs into Headers: a Headers passes through, a single Pair or a vector/tuple of pairs is converted (string keys/values are Stringed; anything else throws ArgumentError).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.mergeheaders","page":"API","location":"api.html#Mongoose.Kernel.mergeheaders","category":"function","text":"mergeheaders(headers::Headers, extra; prepend=false) → Headers\nmergeheaders(response::Response, extra; prepend=false) → Response\n\nReturn a new headers list (or a new Response carrying it) with extra merged in; the original is left untouched. extra may be a single pair or a vector of pairs.\n\nThis is a concatenation, not a dictionary merge: existing pairs are preserved (including duplicates and order). extra is appended after them by default; prepend=true places extra first so get/haskey find those values before any same-named pair the original carried.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Cookie","page":"API","location":"api.html#Mongoose.Kernel.Cookie","category":"type","text":"Cookie — HTTP Set-Cookie parameters.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.json","page":"API","location":"api.html#Mongoose.Kernel.json","category":"function","text":"json(data; status=200, headers=[]) → Response\n\nCreate a JSON response. Supports Dict, NamedTuple, Vector, and any JSON-serializable type.\n\nExample\n\njson(Dict(\"id\" => 1, \"name\" => \"Alice\"))\njson((id=1, name=\"Alice\"))  # NamedTuple\njson(Dict(\"error\" => \"Not Found\"); status=404)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.html","page":"API","location":"api.html#Mongoose.Kernel.html","category":"function","text":"html(content; status=200, headers=[]) → Response\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.text","page":"API","location":"api.html#Mongoose.Kernel.text","category":"function","text":"text(content; status=200, headers=[]) → Response\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.redirect","page":"API","location":"api.html#Mongoose.Kernel.redirect","category":"function","text":"redirect(url; status=302, headers=[]) → Response\n\nReturn an HTTP redirect response.\n\nExample\n\nredirect(\"/login\")\nredirect(\"https://example.com\"; status=301)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.setcookie","page":"API","location":"api.html#Mongoose.Kernel.setcookie","category":"function","text":"setcookie(cookie) → String\n\nSerialize a Cookie to a Set-Cookie header value string.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.query","page":"API","location":"api.html#Mongoose.Kernel.query","category":"function","text":"query(req, key) → Union{String, Nothing}\nquery(req, key, default) → String\nquery(req, key, default::T) → T  (auto-parses to type of default)\n\nType-safe query parameter access with optional default and auto-parsing.\n\nExamples\n\nq = query(req, \"q\", \"\")           # String with default\npage = query(req, \"page\", 1)      # Auto-parse to Int\nlimit = query(req, \"limit\", 20)   # Auto-parse to Int\nflag = query(req, \"debug\", false) # Auto-parse to Bool\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.body","page":"API","location":"api.html#Mongoose.Kernel.body","category":"function","text":"body(req) → String\n\nReturn the raw request body.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.parsejson","page":"API","location":"api.html#Mongoose.Kernel.parsejson","category":"function","text":"parsejson(req) → Any\n\nParse the request body as JSON. Throws BadRequestError (400) on malformed JSON so the pipeline answers 400 instead of falling through to a 500.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.parseform","page":"API","location":"api.html#Mongoose.Kernel.parseform","category":"function","text":"parseform(req) → Dict{String,String}\n\nParse an application/x-www-form-urlencoded request body.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.parsemultipart","page":"API","location":"api.html#Mongoose.Kernel.parsemultipart","category":"function","text":"parsemultipart(req) → Dict{String, Union{String, MultipartFile}}\n\nParse a multipart/form-data request body. Returns a Dict where string fields map to their values and file fields map to MultipartFile objects.\n\nExample\n\npost!(app, \"/upload\") do req\n    parts = parsemultipart(req)\n    file = parts[\"avatar\"]::MultipartFile\n    text(\"Received $(file.filename) ($(length(file.data)) bytes)\")\nend\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.MultipartFile","page":"API","location":"api.html#Mongoose.Kernel.MultipartFile","category":"type","text":"MultipartFile — Represents a file uploaded via multipart/form-data.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.parsecookies","page":"API","location":"api.html#Mongoose.Kernel.parsecookies","category":"function","text":"parsecookies(req) → Dict{String,String}\n\nParse cookies from the request Cookie header.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.context","page":"API","location":"api.html#Mongoose.Kernel.context","category":"function","text":"context(req) → Dict{Symbol,Any}\n\nReturn the request's per-request context dict, creating it lazily on first access.\n\nExample\n\nget!(app, \"/\") do req\n    ctx = context(req)\n    ctx[:user_id] = 42\n    json(Dict(\"ok\" => true))\nend\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.header","page":"API","location":"api.html#Mongoose.Kernel.header","category":"function","text":"header(req, name) → Union{String,Nothing}\n\nLook up a request header by name (case-insensitive).\n\n\n\n\n\n"},{"title":"Mongoose.service","page":"API","location":"api.html#Mongoose.service","category":"function","text":"service(req, name) → Any\nservice(req, ::Val{name}) → T\nservice(req, name, T) → T\n\nRetrieve a service by name from the request context.\n\nservice(req, :db) returns the raw value (values may be zero-arg callables, which are invoked).\nservice(req, Val(:db)) avoids re-parsing the name, but the lookup goes through the dynamic request context — the return type is inferred Any.\nservice(req, :db, DBPool) asserts the type and throws otherwise.\n\nFor type-stable access in hot paths use withservices, whose closure receives the concrete NamedTuple:\n\nExample\n\napp = App(services=(db=pool, cache=redis))\nwithservices(req) do svcs\n    svcs.db            # concrete: DBPool\nend\ndb = service(req, Val(:db))   # convenient, dynamically typed\n\n\n\n\n\n"},{"title":"Mongoose.services","page":"API","location":"api.html#Mongoose.services","category":"function","text":"services(req) → NamedTuple\n\nThe request's DI services as a NamedTuple (empty when none were registered). Dynamically typed at this boundary; use withservices for type-stable access.\n\n\n\n\n\n"},{"title":"Mongoose.withservices","page":"API","location":"api.html#Mongoose.withservices","category":"function","text":"withservices(f, req) → f(services(req))\n\nFunction-barrier access to DI services: the closure receives the concrete NamedTuple, so field access inside it specializes — unlike service(req, Val(:x)), which returns a dynamically typed value.\n\nExample\n\nwithservices(req) do svcs\n    svcs.db.query(\"select 1\")\nend\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.parsequery","page":"API","location":"api.html#Mongoose.Kernel.parsequery","category":"function","text":"parsequery(query::AbstractString) → Dict{String,String}\n\nSingle-pass query string parser. Pre-sized for typical query parameter counts.\n\n\n\n\n\nparsequery(req) → Dict{String,String}\n\nThe request's query parameters, parsed from the raw query on first access and memoized. Prefer query for typed lookups; use this when you need the whole dict. (The string method parsequery(str) is the underlying parser.)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.stripquery","page":"API","location":"api.html#Mongoose.Kernel.stripquery","category":"function","text":"stripquery(uri) → SubString\n\nReturn the URI path without the query string. Zero-allocation (returns a view).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.formatheaders","page":"API","location":"api.html#Mongoose.Kernel.formatheaders","category":"function","text":"formatheaders(headers) → String\n\nSerialize headers into Mongoose C library format: \"Key: Value\\r\\n\".\n\n\n\n\n\nformatheaders(io, headers)\n\nWrite the Mongoose header framing for headers into io — used to assemble a response header block in one buffer (no intermediate String).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.urldecode","page":"API","location":"api.html#Mongoose.Kernel.urldecode","category":"function","text":"urldecode(bytes, start_i, end_i; plus=true) → String\n\nDecode a URL-encoded byte range. Handles + → space (form semantics) and %XX hex escapes. Pass plus=false for path segments, where + is a literal character (RFC 3986 §2.3).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.asstrings","page":"API","location":"api.html#Mongoose.Kernel.asstrings","category":"function","text":"asstrings(input) → Vector{String}\n\nNormalize string-ish input into a Vector{String}: nothing → empty, a single AbstractString → one-element vector, a vector/tuple → Stringed entries. Used for paths=, allowed_origins=, and similar collection kwargs.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.statusreason","page":"API","location":"api.html#Mongoose.Kernel.statusreason","category":"function","text":"HTTP status code → reason phrase mapping.\nInlined chain for hot-path status codes.\n\n\n\n\n\n"},{"title":"Mongoose.use!","page":"API","location":"api.html#Mongoose.use!","category":"function","text":"use!(app, middleware; paths=[])\n\nAdd middleware to an app. middleware may be any callable (req, next) → Response or an AbstractMiddleware subtype; plain functions are wrapped automatically. When paths is non-empty, the middleware only applies to requests whose URI starts with one of the given prefixes.\n\nExample\n\nuse!(app, cors())\nuse!(app, bearer(validate_token); paths=[\"/api\"])\nuse!(app, (req, next) -> (req.headers ...; next()))\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.cors","page":"API","location":"api.html#Mongoose.Kernel.cors","category":"function","text":"cors(; origins, allow_methods, allow_headers, max_age_seconds, allow_credentials)\n\nCreate a CORS middleware. origins accepts \"*\", a single origin, or a Vector{String} of allowed origins.\n\nKeyword Arguments\n\norigins: Allowed origins (default \"*\"; a list enables per-origin echo).\nallow_methods::String: Allowed HTTP methods (default \"GET, POST, PUT, PATCH, DELETE, OPTIONS\").\nallow_headers::String: Allowed request headers (default \"Content-Type, Authorization\").\nmax_age_seconds::Int: Preflight cache duration in seconds (default 86400).\nallow_credentials::Bool: Emit Access-Control-Allow-Credentials (default false).\n\nExample\n\nuse!(app, cors(origins=[\"https://myapp.com\", \"https://admin.myapp.com\"],\n               allow_credentials=true, max_age_seconds=600))\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.ratelimit","page":"API","location":"api.html#Mongoose.Kernel.ratelimit","category":"function","text":"ratelimit(; max_requests, window_seconds, trust_proxies, key_fn)\n\nCreate a rate-limiting middleware using a sharded fixed-window counter. Returns 429 Too Many Requests when the limit is exceeded.\n\nUses 16 independent shards internally to minimize lock contention.\n\nKeyword Arguments\n\nmax_requests::Int: Maximum requests allowed per window (default: 100).\nwindow_seconds::Int: Time window duration in seconds (default: 60).\ntrust_proxies::Bool: Trust X-Forwarded-For/X-Real-IP for the default bucket key (default: false). Enable only behind a proxy that overwrites these headers; otherwise clients can spoof their bucket. Without it the default key is the request's remote address (per-client host).\nkey_fn::Function: Custom bucket key (Request) -> String, overriding the default IP-based key (e.g. an API key extracted from the request).\n\nExample\n\nuse!(server, ratelimit(max_requests=50, window_seconds=30))\nuse!(server, ratelimit(max_requests=100, key_fn=req -> apikey(req)))\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.bearer","page":"API","location":"api.html#Mongoose.Kernel.bearer","category":"function","text":"bearer(validator)\nbearer(secret::String)\n\nCreate a Bearer token authentication middleware. When called with a function, validator(token) must return true if valid. When called with a string, uses constant-time comparison to prevent timing attacks.\n\nExample\n\nuse!(server, bearer(\"my-secret-token\"))\nuse!(server, bearer(token -> token in valid_tokens))\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.apikey","page":"API","location":"api.html#Mongoose.Kernel.apikey","category":"function","text":"apikey(keys; header_name=\"X-API-Key\")\napikey(; header_name=\"X-API-Key\", keys)\n\nCreate an API key authentication middleware. keys may be a single key string or a collection of keys (Set/Vector); it is normalized to a Set{String} at construction. The positional and keyword forms are equivalent.\n\nExample\n\nuse!(server, apikey(\"key-123\"))\nuse!(server, apikey(Set([\"key-123\", \"key-456\"])))\nuse!(server, apikey([\"key-123\"]); header_name=\"x-api-key\")\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.basicauth","page":"API","location":"api.html#Mongoose.Kernel.basicauth","category":"function","text":"basicauth(user, password; realm=\"restricted\")\n\nCreate an HTTP Basic authentication middleware with constant-time credential comparison.\n\nExample\n\nuse!(server, basicauth(\"admin\", ENV[\"ADMIN_PASSWORD\"]))\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.logger","page":"API","location":"api.html#Mongoose.Kernel.logger","category":"function","text":"logger(; threshold_ms=0, output=stderr, structured=false)\n\nCreate a request-logging middleware.\n\nKeyword Arguments\n\nthreshold_ms::Int: Only log requests slower than this (default: 0 = log all) in milliseconds.\noutput::IO: IO stream for log output (default: stderr).\nstructured::Bool: If true, emit one JSON object per line (default: false).\n\nExample\n\nuse!(server, logger())                         # plain text, all requests\nuse!(server, logger(threshold_ms=100))         # only slow requests\nuse!(server, logger(structured=true))          # JSON structured logs\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.health","page":"API","location":"api.html#Mongoose.Kernel.health","category":"function","text":"health(; health_check, ready_check, live_check)\n\nCreate a health check middleware for cloud-native deployments.\n\nKeyword Arguments\n\nhealth_check::Function: Function that returns true if the service is healthy (default: always true)\nready_check::Function: Function that returns true if the service is ready to accept traffic (default: always true)\nlive_check::Function: Function that returns true if the service is alive (default: always true)\n\nEndpoints\n\nGET /healthz: Overall health status (combines all checks)\nGET /readyz: Readiness for traffic (load balancers)\nGET /livez: Liveness check (process alive)\n\nExample\n\nuse!(server, health(\n    health_check = () -> check_database(),\n    ready_check = () -> check_dependencies(),\n    live_check = () -> true  # Process is always alive if running\n))\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.metrics","page":"API","location":"api.html#Mongoose.Kernel.metrics","category":"function","text":"metrics(; path=\"/metrics\")\n\nCreate a Prometheus-compatible metrics middleware.\n\nExposes an HTTP endpoint (default /metrics) in Prometheus text exposition format v0.0.4. Uses 8 internal shards keyed by thread ID to minimize lock contention under concurrent load.\n\nKeyword Arguments\n\npath::String: Scrape endpoint path (default: \"/metrics\").\n\nMetrics\n\nMetric Type Labels\nhttp_requests_total counter method, status\nhttp_request_duration_seconds histogram le (11 buckets: 5ms–10s)\nmongoose_connections gauge open connections\nmongoose_ws_clients gauge open WebSocket clients\nmongoose_active_streams gauge in-flight streaming responses\nmongoose_executor_inflight gauge jobs currently executing\nmongoose_executor_queue_depth gauge jobs waiting in the executor queue\n\nGauges are emitted once the middleware is registered (use! attaches the server); before that only the counter and histogram are exposed.\n\nExample\n\napp = App(workers=4)\nuse!(app, health())\nuse!(app, metrics())   # exposes GET /metrics\n\nstart!(app; port=8080)\n\nPrometheus scrape_configs:\n\n- job_name: myapp\n  static_configs:\n    - targets: ['localhost:8080']\n  metrics_path: /metrics\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.security","page":"API","location":"api.html#Mongoose.Kernel.security","category":"function","text":"security(; hsts_max_age_seconds, frame_options, content_type_options, referrer_policy, csp)\n\nCreate a security headers middleware. All headers are pre-computed at construction time. Each optional header is disabled with nothing (uniform \"off\" convention); only content_type_options is a plain Bool.\n\nKeyword Arguments\n\nhsts_max_age_seconds::Union{Nothing,Int}: HSTS max-age in seconds (default: 31536000 = 1 year). nothing disables the header.\nframe_options::Union{Nothing,String}: X-Frame-Options value (default: \"DENY\").\ncontent_type_options::Bool: Add X-Content-Type-Options: nosniff (default: true).\nreferrer_policy::Union{Nothing,String}: Referrer-Policy value (default: \"strict-origin-when-cross-origin\").\ncsp::Union{Nothing,String}: Content-Security-Policy value (default: nothing = not added).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.compress","page":"API","location":"api.html#Mongoose.Kernel.compress","category":"function","text":"compress(; min_size_bytes=1024) → Compress\n\nCreate a GZip compression middleware. Only compresses responses larger than min_size_bytes bytes when the client sends Accept-Encoding: gzip.\n\nKeyword Arguments\n\nmin_size_bytes::Int: Minimum response body size to compress (default: 1024 bytes).\n\nExample\n\nuse!(app, compress())\nuse!(app, compress(min_size_bytes=256))  # More aggressive compression\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.etag","page":"API","location":"api.html#Mongoose.Kernel.etag","category":"function","text":"etag() — add ETag + conditional request middleware (304/412).\n\n# Example\n```julia\nuse!(app, etag())                    # before compress, after cors\nuse!(app, compress(min_size_bytes=1024))\n```\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.AbstractMiddleware","page":"API","location":"api.html#Mongoose.Kernel.AbstractMiddleware","category":"type","text":"Middleware protocol — composable request/response pipeline.\n\nMiddleware is a callable `(req, next) → response` that wraps around the\nhandler (\"onion\" model). `req` is the `Request`, `next` advances to the\nfollowing middleware (and finally the handler); returning a `Response`\nwithout calling `next` short-circuits.\n\n# Implementing Middleware\n\nSubtype `AbstractMiddleware` and implement the call operator:\n\n```julia\nstruct MyMiddleware <: AbstractMiddleware end\n\nfunction (mw::MyMiddleware)(req::Request, next::Function)\n    # before logic\n    response = next()  # call downstream\n    # after logic\n    return response\nend\n```\n\nPlain closures/functions work too: `use!`/`route!` wrap them via\n`asmiddleware` (see `FunctionMiddleware`). The tag type exists so the\npipeline can hold a typed stack (`Vector{AbstractMiddleware}`).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.FunctionMiddleware","page":"API","location":"api.html#Mongoose.Kernel.FunctionMiddleware","category":"type","text":"FunctionMiddleware{F} — adapter that lets any callable `f(req, next)` act\nas a middleware without subtyping `AbstractMiddleware`.\n\nUser code rarely needs this directly: `use!` and `route!(; middleware=...)`\naccept plain closures/functions and wrap them automatically.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Logger","page":"API","location":"api.html#Mongoose.Kernel.Logger","category":"type","text":"Logger — Request logging middleware.\nLogs each request's method, URI, status code, and elapsed time.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Health","page":"API","location":"api.html#Mongoose.Kernel.Health","category":"type","text":"Health — Health check middleware for cloud-native deployments.\nIntercepts `/healthz`, `/readyz`, and `/livez` before any other middleware.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Metrics","page":"API","location":"api.html#Mongoose.Kernel.Metrics","category":"type","text":"Metrics — Prometheus-compatible metrics middleware.\n\nIntercepts every request, records latency and status, and exposes the\nconfigured `/metrics` endpoint in Prometheus text exposition format.\n\nMetrics exposed:\n- `http_requests_total{method,status}` — counter\n- `http_request_duration_seconds{le}` — histogram (11 finite buckets)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Security","page":"API","location":"api.html#Mongoose.Kernel.Security","category":"type","text":"Security — adds standard security headers to all responses.\n\nProtects against common web vulnerabilities (XSS, clickjacking, MIME\nsniffing).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Etag","page":"API","location":"api.html#Mongoose.Kernel.Etag","category":"type","text":"Etag — middleware that computes strong ETags and handles conditional requests.\n\nAdds an `ETag` header to generated buffered responses (deterministic\nFNV-1a over the response body bytes — stable across restarts, no new\ndependencies), and applies RFC 9110 §13 preconditions:\n\n- `If-None-Match` match → `304 Not Modified` (GET/HEAD) or\n  `412 Precondition Failed` (other methods); `\"*\"` matches any tag.\n- `If-Match` mismatch → `412 Precondition Failed` (strong comparison).\n- `If-None-Match` uses weak comparison (`W/` prefixes are stripped).\n\nStreaming responses and raw (non-`Response`) handler returns are passed\nthrough unchanged — they have no serialized body to hash. Register it\noutside transformation middleware (e.g. before `compress`) so the ETag\nvalidates the representation that is actually sent.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Cors","page":"API","location":"api.html#Mongoose.Kernel.Cors","category":"type","text":"Cors — CORS (Cross-Origin Resource Sharing) middleware.\n\nHandles preflight OPTIONS requests and attaches CORS headers to responses:\n\n- origin allowlist: only listed origins get `Access-Control-Allow-Origin`,\n  reflected verbatim (with `Vary: Origin`); `\"*\"` still means any origin.\n- credentials: opt-in `Access-Control-Allow-Credentials: true`\n  (`\"*\"` + credentials is not emitted, per the spec).\n- preflight validation: requested method and request headers are checked\n  against the allowlists; mismatches get a 403 instead of a blanket 204.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Bearer","page":"API","location":"api.html#Mongoose.Kernel.Bearer","category":"type","text":"Bearer — Bearer token authentication middleware.\nChecks the `Authorization: Bearer <token>` header and delegates validation to a user-supplied function.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.ApiKey","page":"API","location":"api.html#Mongoose.Kernel.ApiKey","category":"type","text":"ApiKey — API key authentication middleware.\nReads a header by name and checks it against a set of valid keys.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.BasicAuth","page":"API","location":"api.html#Mongoose.Kernel.BasicAuth","category":"type","text":"BasicAuth — HTTP Basic authentication middleware.\nValidates `Authorization: Basic <base64(user:password)>` with\nconstant-time comparison.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.RateLimit","page":"API","location":"api.html#Mongoose.Kernel.RateLimit","category":"type","text":"RateLimit — fixed-window rate limiting middleware with sharded locks.\n\nTracks requests per bucket key with automatic cleanup, using N shards\n(each with its own lock) to reduce contention under high concurrency.\n\nBuckets are keyed by a user-supplied `key_fn(request) -> String`, or by a\ndefault that keys on the request's transport-provided remote address;\n`X-Forwarded-For`/`X-Real-IP` are only trusted when `trust_proxies=true`\n(reading proxy headers directly is a spoofing vector when the app is\nexposed to clients).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Compress","page":"API","location":"api.html#Mongoose.Kernel.Compress","category":"type","text":"Compress — GZip compression middleware.\n\nCompresses response bodies when the client supports it\n(Accept-Encoding: gzip). Skips already-compressed, streaming, or small\nresponses.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.SSEWriter","page":"API","location":"api.html#Mongoose.Kernel.SSEWriter","category":"type","text":"SSEWriter — Writes Server-Sent Events to a connection.\n\nUsed within a `StreamResponse` producer:\n```julia\nfunction sse_handler(req)\n    sse(req) do writer\n        for i in 1:10\n            emit(writer; data=\"tick $i\", event=\"heartbeat\", id=string(i))\n            sleep(1.0)\n        end\n    end\nend\n```\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.emit","page":"API","location":"api.html#Mongoose.Kernel.emit","category":"function","text":"emit(sse; data, event=\"\", id=\"\", retry_ms=nothing)\n\nSend a single SSE event to the client. Fields:\n\ndata::String — event payload (required). Multi-line data is handled correctly.\nevent::String — event type/name (optional).\nid::String — event ID for reconnection (optional).\nretry_ms::Union{Nothing,Int} — reconnection interval in ms (optional).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.sse","page":"API","location":"api.html#Mongoose.Kernel.sse","category":"function","text":"sse([req,] producer; headers=[]) → StreamResponse\n\nConvenience constructor for SSE responses with correct headers.\n\nfunction stream_events(req)\n    sse(req) do writer\n        emit(writer; data=\"hello\", event=\"greeting\")\n    end\nend\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Message","page":"API","location":"api.html#Mongoose.Kernel.Message","category":"type","text":"Message — one WebSocket frame.\n\n`data` is either the UTF-8 text of a text frame or the raw bytes of a\nbinary frame. WebSocket handlers receive a `Message` and may return a\n`Message` to reply (or `nothing` for no reply).\n\n\n\n\n\n"},{"title":"Mongoose.broadcastws","page":"API","location":"api.html#Mongoose.broadcastws","category":"function","text":"broadcastws(server, path, data)\n\nServer-initiated WebSocket push: enqueue a text frame for every open client of path (async executors only — the frame is routed through the same reply queue the worker pool uses, so it is sent on the poll/callback thread and is safe to call from any task). Idle/closed clients are skipped naturally: a stale conn id is dropped when drained.\n\n\n\n\n\n"},{"title":"Mongoose.onerror!","page":"API","location":"api.html#Mongoose.onerror!","category":"function","text":"onerror!(app, status, handler)\nonerror!(handler, app, status)\n\nRegister a custom error handler for a specific HTTP status code. handler may be a Response (static) or Function(req) → Response (dynamic). Both argument orders are accepted; the (handler, app, status) form exists so a do-block works.\n\nExample\n\nonerror!(app, 404) do req\n    json(Dict(\"error\" => \"not found\", \"path\" => req.uri); status=404)\nend\n\n\n\n\n\nonerror!(app, ::Type{E}, handler)\n\nRegister a typed exception handler: handler(req, e) returns the Response for any handler/middleware error that is a E (or subtype). Handlers are tried in registration order; unhandled exceptions fall through to the default 500 path.\n\nExample\n\nstruct NoMatch <: Exception end\nonerror!(app, NoMatch) do req, e\n    json(Dict(\"error\" => \"not found\"); status=404)\nend\n\n\n\n\n\n"},{"title":"Mongoose.onstart!","page":"API","location":"api.html#Mongoose.onstart!","category":"function","text":"onstart!(app, f)\nonstart!(f, app)\n\nRegister a callback to run after the server starts (before accepting connections). Both argument orders are accepted; the (f, app) form exists so onstart!(app) do … end works.\n\n\n\n\n\n"},{"title":"Mongoose.onstop!","page":"API","location":"api.html#Mongoose.onstop!","category":"function","text":"onstop!(app, f)\nonstop!(f, app)\n\nRegister a callback to run during graceful shutdown. Both argument orders are accepted; the (f, app) form exists so onstop!(app) do … end works.\n\n\n\n\n\n"},{"title":"Mongoose.service!","page":"API","location":"api.html#Mongoose.service!","category":"function","text":"service!(app, name, value)\n\nRegister a service for dependency injection.\n\nservice!(app, :db, MyDB.connect())\nservice(req, :db)      # retrieve inside handler\n\n\n\n\n\n"},{"title":"Mongoose.background!","page":"API","location":"api.html#Mongoose.background!","category":"function","text":"background!(app, f)\nbackground!(f, app)\n\nSchedule a background task to be spawned when start! is called. f should be a zero-argument function. Both argument orders are accepted; the (f, app) form exists so background!(app) do … end works.\n\nbackground!(app) do\n    while true\n        cleanup_expired_sessions!()\n        sleep(60)\n    end\nend\n\n\n\n\n\n"},{"title":"Mongoose.serve!","page":"API","location":"api.html#Mongoose.serve!","category":"function","text":"serve!(server, directory; uri_prefix=\"/\")\n\nServe static files from directory under uri_prefix on the URL. The directory is the positional argument; the URL prefix is always the keyword (so the two can never be silently swapped).\n\n\n\n\n\n"},{"title":"Mongoose.ServerConfig","page":"API","location":"api.html#Mongoose.ServerConfig","category":"type","text":"ServerConfig — Immutable tuning parameters for the server.\n\nSeparated from `App` runtime state to clearly distinguish what's\nconfiguration (set once) vs what changes during execution.\n\n\n\n\n\n"},{"title":"Mongoose.TLSConfig","page":"API","location":"api.html#Mongoose.TLSConfig","category":"type","text":"TLSConfig — TLS options for HTTPS/WSS servers.\n\n`cert`, `key`, and `ca` accept: file paths, PEM strings, or raw bytes.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.AbstractRouter","page":"API","location":"api.html#Mongoose.Kernel.AbstractRouter","category":"type","text":"AbstractRouter — pluggable routing protocol.\n\nAny type `R <: AbstractRouter` can be supplied to `App(router=R())`.\nThe transport and dispatch layers talk to routers exclusively through\nthis protocol; they never inspect internal fields.\n\nRequired protocol for HTTP dispatch:\n- `route!(r::R, method, path, handler; middleware=AbstractMiddleware[], metadata=nothing) → r`\n  (register an HTTP route; the router stores the handler inside an\n  `Endpoint` and never interprets it. The `App` layer normalizes\n  `middleware` to a vector before delegating, so a router may assume an\n  `AbstractVector`)\n- `matchroute(r::R, method, path)`                → a `RouteResult`\n  (`Matched` / `NoMatch` / `MethodMismatch{allowed}` — 404/405 and the\n  `Allow` set are resolved by the router at match time)\n- `hasroute(r::R, path)`                  → `Bool` (path owned, catch-all excluded)\n  (no `\"*\"` fallback — route ownership check for static serving)\n- `gethandler(match, method)` / `getendpoint(match, method)` — work on\n  any `Matched`, including custom routers' matches and `SingleEndpoint`\n\nCustom routers return a `RouteResult` from `matchroute`. Returning a\n`Matched` directly is enough: wrap the endpoint with\n`SingleEndpoint(ep, method)` as the match's `handlers` value.\n\nOptional capabilities (safe defaults are provided):\n- `haswsroutes(r::R) → Bool`                   (default: `false`)\n- `ws!(r::R, path; ...)`                         (no default)\n- `getwsendpoint(r::R, uri)`                    (default: `nothing`)\n- `length(r::R)`                            (default: `0`)\n\nThe default implementation is `Router` in `router.jl`.\n\n## Contract-by-fallback pattern\n\nThe `AbstractRouter` protocol is enforced by *fallback methods*: calling a\nprotocol function on an unimplemented router type throws a `MethodError`\nnaming the missing method, so a partially-implemented router fails loudly\ninstead of silently under-dispatching. Optional capabilities default to\n\"not supported\" rather than throwing.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.SingleEndpoint","page":"API","location":"api.html#Mongoose.Kernel.SingleEndpoint","category":"type","text":"SingleEndpoint — minimal \"handlers\" carrier for custom routers that don't\ntrack per-method maps. `gethandler`/`getendpoint` answer for exactly the\none method the endpoint serves.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Endpoint","page":"API","location":"api.html#Mongoose.Kernel.Endpoint","category":"type","text":"Endpoint{F} — what a route owns: handler, scoped middleware, and metadata.\n\nThe handler's concrete type is a type parameter, so the endpoint can be\ninvoked without a dynamic call when its type is known (custom routers,\ncompiled terminals); registration specializes on the handler type.\n\nThe router only stores and returns `Endpoint`s; it does not execute\nmiddleware. `middleware` applies to this route (in addition to app-global\nmiddleware); `metadata` is opaque and available for OpenAPI-style docs.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.AbstractExecutor","page":"API","location":"api.html#Mongoose.Kernel.AbstractExecutor","category":"type","text":"AbstractExecutor — controls where and how middleware + handlers run.\n\nThe executor is the \"where/how to run\" half of the runtime. It owns the\nconcurrency policy (inline vs worker pool), backpressure, and lifecycle of\nexecution, so the transport and `App` never see `Channel`s or `Task`s.\n\nHandler jobs are closures `() → Union{Nothing, <:Tagged}` producing the\nreply to deliver (or `nothing` to drop). The executor only runs them and\nships replies; it never interprets `Request`/`Response`/`Intent`.\n\nReference implementations:\n- `SyncExecutor` — runs each job inline on the calling thread.\n- `AsyncExecutor` (server layer) — bounded worker pool + reply queue.\n\n## Contract\n\n- `submit!(executor, job)` — schedule a job. `SyncExecutor` runs it\n  inline and returns its value; pool executors return nothing.\n- `start!(executor, app)` / `stop!(executor)` — lifecycle.\n- `haspending(executor)` — is work still in flight? (drain support)\n\nMissing methods fail loudly via fallback methods below.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.SyncExecutor","page":"API","location":"api.html#Mongoose.Kernel.SyncExecutor","category":"type","text":"SyncExecutor — runs every job inline on the calling thread.\n\nThis is the transport-agnostic equivalent of `workers=0` sync mode.\n\n\n\n\n\n"},{"title":"Mongoose.AsyncExecutor","page":"API","location":"api.html#Mongoose.AsyncExecutor","category":"type","text":"AsyncExecutor — bounded worker pool with a reply queue.\n\nArchitecture:\n- Transport submits closures (`() → Union{Nothing,Kernel.Tagged}`) via `submit!`.\n- `workers` threads dequeue and run jobs, pushing replies onto `replies`.\n- The event loop drains `replies` (see `dispatch_replies!`) and sends them\n  through the transport's connections.\n- `supervise_workers!` respawns dead workers on a timer.\n\nThe executor never interprets requests/responses; it only runs jobs and\nships the replies they produce. Timeout policy is applied by the transport\nwhen it builds a job.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.stop!","page":"API","location":"api.html#Mongoose.Kernel.stop!","category":"function","text":"stop!(exec::AsyncExecutor; timeout=5.0)\n\nStop the worker pool. calls is closed first, then replies are drained while workers are joined so a full reply queue cannot deadlock the join. The join is bounded by timeout seconds: a handler that never returns is abandoned (its task keeps running, as before) instead of hanging shutdown forever.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.FakeExecutor","page":"API","location":"api.html#Mongoose.Kernel.FakeExecutor","category":"type","text":"FakeExecutor — deterministic, thread-free executor for tests.\n\nMirrors the `AsyncExecutor` contract (queued `submit!`, `haspending`,\n`start!`/`stop!`) but never spawns workers: jobs are queued and only run\nwhen the test calls `run!`, inline in submission order. This makes\nreply-order/backpressure tests deterministic — no threads, no sleeps.\n\n```julia\nfe = FakeExecutor()\nsubmit!(fe, () -> \"first\")\nsubmit!(fe, () -> \"second\")\n@assert haspending(fe)\n@assert run!(fe) == [\"first\", \"second\"]\n@assert !haspending(fe)\n```\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.run!","page":"API","location":"api.html#Mongoose.Kernel.run!","category":"function","text":"run!(fe::FakeExecutor) → Vector{Any}\n\nRun every queued job inline in submission order, clearing the queue and recording each result.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.AbstractTransport","page":"API","location":"api.html#Mongoose.Kernel.AbstractTransport","category":"type","text":"AbstractTransport — replaceable I/O transport.\n\nThe transport is the last FFI boundary: it converts external events into\n`Kernel` objects and responses into bytes/frames, and the runtime\nnever sees `Ptr{Cvoid}`.\n\nToday the transport is a **capability-tagged seam** rather than a callable\ninterface: implementations declare what they can do via the ability\ntraits (`canws`, `cantls`, `canstream`), and the server\ndrives the one C implementation\n(`transport/mongoose`). A reference fake (`FakeTransport`, in\n`testing.jl`) drives the whole pipeline with no FFI, which is what\n`FakeTransport` uses.\n\nThe C transport's concrete lifecycle entry points (used by `start!` /\n`shutdown!`) are `init_server!`, `bind_server!`,\n`spawn_event_loop!`/`stop_event_loop!`, and its send path is\n`send_http_response!`/`send_ws_frame!`/`send_stream_response!` in\n`transport/mongoose`. Extracting a full `init!/listen!/poll!/send!`\ninterface behind `AbstractTransport` is tracked as deferred work\n(see WORKLOG, T9): there is a single real implementation today, and the\ntrait seam already delivers the replaceability guarantee.\n\n# Capability traits\n\nOptional features are detected via trait functions and never assumed\nby the runtime: `canws`, `cantls`,\n`canstream`. A transport that cannot do WebSocket simply reports\n`false`; HTTP-only apps keep working.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.canws","page":"API","location":"api.html#Mongoose.Kernel.canws","category":"function","text":"canws(transport) → Bool\n\nWhether the transport supports WebSocket upgrades. Defaults to false; implementations override it for their capability set.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.cantls","page":"API","location":"api.html#Mongoose.Kernel.cantls","category":"function","text":"cantls(transport) → Bool\n\nWhether the transport supports TLS. Defaults to false.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.canstream","page":"API","location":"api.html#Mongoose.Kernel.canstream","category":"function","text":"canstream(transport) → Bool\n\nWhether the transport supports streamed (chunked) responses. Defaults to false.\n\n\n\n\n\n"},{"title":"Mongoose.FakeTransport","page":"API","location":"api.html#Mongoose.FakeTransport","category":"type","text":"FakeTransport — reference transport that runs the pipeline with no FFI.\n\nA `FakeTransport` really is a fake transport: it dispatches requests directly\nthrough the middleware pipeline and router (`process`), bypassing\nthe C event loop entirely. It declares its capabilities via the ability\ntraits (`canws`, `cantls`, `canstream`): no WebSocket,\nno TLS, streaming supported. It can\ndrive a full request cycle without a running server — including on systems\nwhere `Mongoose_jll` was never loaded.\n\nThe transport is **stateful**: it owns a registry of in-flight streams\n(`transport.streams`). A streamed response is bound to exactly one\n`FakeStream` — after it is delivered (producer completes) the stream is\n`done`, and any further write raises `StreamClosedError` (one response per\nstream). `close!(transport)` closes every owned stream (writers become\nclosed) and rejects new requests, modeling the C transport's close cascade.\n\n# Example\n```julia\napp = App()\nget!(app, \"/hello\") do req\n    json((message=\"Hello World\",))\nend\n\nclient = FakeTransport(app)\nresp = client(:get, \"/hello\")\n@assert resp.status == 200\n@assert contains(resp.body, \"Hello World\")\n```\n\n\n\n\n\n"},{"title":"Mongoose.close!","page":"API","location":"api.html#Mongoose.close!","category":"function","text":"close!(transport::FakeTransport)\n\nClose the transport: every owned stream is closed (its writer becomes closed) and further requests raise an error.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.validate","page":"API","location":"api.html#Mongoose.Kernel.validate","category":"function","text":"validate(req, ::Type{T}; on_error=nothing) → T | Response\n\nParse and validate the JSON request body into type T. T must be a struct with a keyword constructor or accept a Dict.\n\nReturns the parsed struct, or throws ValidationError if parsing fails. With on_error, a callback (e::ValidationError) → Response is called with the error instead of throwing; the do-block form validate(req, T) do e … end is sugar for on_error=e.\n\nExample\n\nstruct CreateUser\n    name::String\n    email::String\n    age::Int\nend\n\npost!(app, \"/users\") do req\n    user = validate(req, CreateUser) do e\n        json((error=e.message, field=e.field); status=422)\n    end\n    json((id=1, name=user.name))\nend\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.ValidationError","page":"API","location":"api.html#Mongoose.Kernel.ValidationError","category":"type","text":"ValidationError — Thrown when request body fails validation.\n\nAn unhandled ValidationError maps to a 422 Unprocessable Entity response by default at the transport boundary (override with onerror!(app, ValidationError) do req, e … end).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Plain","page":"API","location":"api.html#Mongoose.Kernel.Plain","category":"type","text":"Plain — `text/plain; charset=utf-8` content format.\n\nBuilt-in format marker types (all subtypes of `AbstractFormat`) are\ndispatched by the type system for zero-cost content-type resolution. New\nformats are added by subtyping `AbstractFormat` and defining `mime` /\n`encode` / `decode`.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Html","page":"API","location":"api.html#Mongoose.Kernel.Html","category":"type","text":"text/html; charset=utf-8 content format. See Plain.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Json","page":"API","location":"api.html#Mongoose.Kernel.Json","category":"type","text":"application/json; charset=utf-8 content format. See Plain.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Css","page":"API","location":"api.html#Mongoose.Kernel.Css","category":"type","text":"text/css; charset=utf-8 content format. See Plain.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Js","page":"API","location":"api.html#Mongoose.Kernel.Js","category":"type","text":"application/javascript; charset=utf-8 content format. See Plain.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Xml","page":"API","location":"api.html#Mongoose.Kernel.Xml","category":"type","text":"application/xml; charset=utf-8 content format. See Plain.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.Binary","page":"API","location":"api.html#Mongoose.Kernel.Binary","category":"type","text":"application/octet-stream content format. See Plain.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.encode","page":"API","location":"api.html#Mongoose.Kernel.encode","category":"function","text":"encode(Format, body) → String / Vector{UInt8}\n\nConvert body to the wire representation for Format. Extend for custom formats:\n\nMongoose.encode(::Type{MyFormat}, body) = serialize(body)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.decode","page":"API","location":"api.html#Mongoose.Kernel.decode","category":"function","text":"decode(Format, body::String) → Any\n\nParse a request body from its wire representation. Extend for custom formats:\n\nMongoose.decode(::Type{MyFormat}, body::String) = deserialize(body)\n\n\n\n\n\n"},{"title":"Mongoose.Kernel","page":"API","location":"api.html#Mongoose.Kernel","category":"module","text":"Kernel — transport-agnostic core of Mongoose.jl.\n\nContains everything that does NOT depend on a server or an FFI transport:\nprotocol types (Request, Response, Headers, formats, cookies, WS types,\nvalidation), the pluggable router, the middleware protocol + built-in\nmiddleware, and the URL/string utilities.\n\nThis module is self-contained and can be loaded and tested without the C\nMongoose library: `using Mongoose.Kernel`.\n\nThe availability of this layer is what makes the transport replaceable —\nanything in `Kernel` can be exercised with pure Julia (see\n`Kernel.process` for the request→response seam).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.HTTPError","page":"API","location":"api.html#Mongoose.Kernel.HTTPError","category":"type","text":"HTTPError{status} <: Exception\n\nException that maps to an HTTP error response. The status code is a compile-time constant type parameter, so errorstatus(e) is free and onerror!(app, NotFoundError) (or onerror!(app, HTTPError{404})) registers a handler for exactly that status.\n\nThrow it inside a handler (or middleware) to signal a non-200 reply with a custom message:\n\nget!(app, \"/users/:id::Int\") do req, id\n    id in 1:999 || throw(NotFoundError(\"user $id not found\"))\n    json((id=id,))\nend\n\nA thrown HTTPError is mapped to Response(status, headers, message) automatically at the transport boundary, unless a more specific onerror! handler (checked first) or a custom onerror!(app, status, …) error page takes precedence.\n\nNamed aliases are provided for every standard status from 400 to 511 (RFC 9110 plus the common extensions): BadRequestError, UnauthorizedError, ForbiddenError, NotFoundError, MethodNotAllowedError, RequestHeaderFieldsTooLargeError, PayloadTooLargeError, UnsupportedMediaTypeError, UnprocessableEntityError, TooManyRequestsError, InternalServerError, HTTPVersionNotSupportedError, … One deliberate gap: 501 has no alias because NotImplementedError is a Base type — use HTTPError{501}.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.errorstatus","page":"API","location":"api.html#Mongoose.Kernel.errorstatus","category":"function","text":"errorstatus(e::HTTPError) → Int\n\nThe HTTP status code carried by e (the compile-time type parameter).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.RouteError","page":"API","location":"api.html#Mongoose.Kernel.RouteError","category":"type","text":"RouteError — Route registration or matching failure.\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.ServerError","page":"API","location":"api.html#Mongoose.Kernel.ServerError","category":"type","text":"ServerError — Server operation failure (initialization, memory, config).\n\n\n\n\n\n"},{"title":"Mongoose.Kernel.BindError","page":"API","location":"api.html#Mongoose.Kernel.BindError","category":"type","text":"BindError — Failed to bind to address/port.\n\n\n\n\n\n"},{"title":"Performance & Deployment","page":"Performance & Deployment","location":"performance.html#Performance-and-Deployment","category":"section","text":"Mongoose.jl is designed so the production configuration is also the fast one: start! freezes and compiles the route table, and middleware runs through a tuple pipeline with no per-request closures. This page collects the measured numbers, the tuning knobs that matter, and how to guard against regressions."},{"title":"Warm-path baselines","page":"Performance & Deployment","location":"performance.html#Warm-path-baselines","category":"section","text":"Measured with bench/dispatch.jl (Julia 1.13, warm, single thread; contexts hoisted out of the measured loop). Treat them as relative baselines, not guarantees — re-run the script on your hardware.\n\nPath Allocations Latency\nprocess frozen fixed route 192 B ~225 ns\nprocess frozen typed-param route 528 B ~400 ns\nprocess frozen + route-scoped middleware 192 B ~350 ns\nprocess frozen + cors() + etag() 1024 B ~1.3 µs\nprocess generic fixed route 256 B ~700 ns\nprocess generic typed-param route 640 B ~1.5 µs\nparse_method 0 B ~4 ns\n\nA handler that returns text(\"ok\")/json(...) costs one Response plus the header block; header-adding middleware costs one mergeheaders (368 B) each. Streams and binary bodies are hand-framed and always close the connection."},{"title":"The production recipe","page":"Performance & Deployment","location":"performance.html#The-production-recipe","category":"section","text":"app = App(;\n    workers            = Threads.nthreads(),  # 0 = sync (inline handlers)\n    queue_size         = 1024,                # 503 backpressure when full\n    request_timeout_ms = 5_000,               # async only\n    drain_timeout_ms   = 5_000,               # graceful shutdown budget\n    max_body_bytes     = 1_048_576,\n    header_timeout_ms  = 10_000,              # slowloris guard\n    max_connections    = 10_000,\n)\n\nuse!(app, security())\nuse!(app, etag())\nuse!(app, compress(min_size_bytes=1024))\nuse!(app, metrics())          # /metrics with counters, histogram, live gauges\nuse!(app, health())           # /healthz /readyz /livez\n\nget!(app, \"/users/:id::Int\") do req, id\n    json((id=id,))\nend\n\nstart!(app; port=8080)        # binds, then freezes/compiles the route table\n\nFreezing is automatic. start! freezes the router after a successful bind, so production gets compiled dispatch without an extra call. A failed start does not freeze, so you can fix routes and retry. Register everything before start!; later registration throws.\nSync or async. workers=0 runs handlers inline (lowest overhead, one slow handler blocks the loop); workers=N runs a bounded pool with backpressure, per-request timeouts, and thread-safe reply delivery. Use async when handlers do I/O.\nScoped middleware is cheap. route!(...; middleware=(a, b)), group(...), and use!(...; paths=[\"/api\"]) all run through the tuple pipeline; route-scoped middleware adds no allocation over an unscoped route.\nDI is typed. App(services=(db=pool,)) sets a typed Request field; read it with withservices(req) do svcs … end for type-stable access.\nLimits protect the loop. header_timeout_ms closes clients that stall before sending a request; max_connections refuses beyond the cap; bodies over max_body_bytes answer 413. Configured body limits above the C receive ceiling (8 MiB on the current build) are rejected at construction."},{"title":"Observability","page":"Performance & Deployment","location":"performance.html#Observability","category":"section","text":"metrics() exposes http_requests_total{method,status}, the latency histogram, and live gauges: mongoose_connections, mongoose_ws_clients, mongoose_active_streams, mongoose_executor_inflight, mongoose_executor_queue_depth. logger() emits access logs (including 500s from throwing handlers) and health() serves Kubernetes probes. Shutdown on SIGINT/SIGTERM drains in-flight requests and SSE streams, runs onstop! hooks, and stops workers."},{"title":"Guarding against regressions","page":"Performance & Deployment","location":"performance.html#Guarding-against-regressions","category":"section","text":"# Print the baseline table (or fail on regressions with BENCH_ASSERT=1)\njulia --project=. bench/dispatch.jl\nBENCH_ASSERT=1 julia --project=. bench/dispatch.jl\n\n# The suite also enforces allocation ceilings and @inferred checks\njulia --project=test test/unit/perf.jl\njulia --project=test test/quality/quality.jl   # Aqua + JET baseline\n\nCI runs the bench as an advisory step and the allocation guards as hard tests. When you touch routing, the pipeline, or the transport, put before/after numbers in the commit message and update the baseline table above plus the julia-performance skill."},{"title":"AOT / juliac --trim","page":"Performance & Deployment","location":"performance.html#AOT-/-juliac-trim","category":"section","text":"freeze! and the compiled route table are the foundation for AOT builds, but juliac --trim support is not complete yet: the trim verifier still finds dynamic dispatch in startup/registration (see WORKLOG.md, \"AOT / trimming readiness\"). Use the standard Julia runtime until that lands."},{"title":"Examples","page":"Examples","location":"examples.html#Examples","category":"section","text":""},{"title":"Hello World","page":"Examples","location":"examples.html#Hello-World","category":"section","text":"using Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> text(\"Hello, World!\"))\n\napp = App(; router=router)\nstart!(app; port=8080)"},{"title":"HTTPS Server (TLS)","page":"Examples","location":"examples.html#HTTPS-Server-(TLS)","category":"section","text":"using Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/secure\", req -> text(\"secure ok\"))\n\napp = App(; router=router, tls=TLSConfig(cert=\"certs/server.crt\", key=\"certs/server.key\"))\nstart!(app; port=8443)"},{"title":"REST API with Typed Parameters","page":"Examples","location":"examples.html#REST-API-with-Typed-Parameters","category":"section","text":"using Mongoose\n\nrouter = Router()\n\n# String parameter (default)\nroute!(router, :get, \"/greet/:name\", (req, name) -> text(\"Hello, $name!\"))\n\n# Typed integer — /users/abc returns 404 automatically\nroute!(router, :get, \"/users/:id::Int\", (req, id) ->\n    json(Dict(\"id\" => id, \"type\" => string(typeof(id))))\n)\n\n# Float parameter\nroute!(router, :get, \"/price/:amount::Float64\", (req, amount) -> begin\n    tax = amount * 0.16\n    json(Dict(\"amount\" => amount, \"tax\" => tax))\nend)\n\n# Wildcard catch-all\nroute!(router, :get, \"/files/*path\", (req, path) ->\n    text(\"Requested: $path\")\n)\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)"},{"title":"Frozen Router (Compiled Dispatch)","page":"Examples","location":"examples.html#Frozen-Router-(Compiled-Dispatch)","category":"section","text":"Once all routes are registered, freeze!(router) closes the table and compiles it: each route's terminal (handler + scoped middleware) is pre-baked with the handler's concrete type captured, and parametric matching walks the raw path with byte indices — no per-request path split, no closure/concat allocation. Dispatch semantics stay identical; only registration is locked.\n\nusing Mongoose\n\nrouter = Router()\n\nget!(router, \"/users/:id::Int\", (req, id) -> json(Dict(\"id\" => id)))\npost!(router, \"/users\", req -> json(Dict(\"created\" => true); status=201))\nget!(router, \"/files/*path\", (req, path) -> text(\"Requested: $path\"))\n\nfreeze!(router)   # route!/ws! throw RouteError from here on\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)\n\nFreezing also provides the closed-table guarantee AOT builds need: with no runtime registration, the route table can be compiled once and pruned. Note that juliac --trim compatibility is not complete yet — the trim verifier still finds dynamic dispatch in startup/registration (see WORKLOG.md, \"AOT / trimming readiness\"). Call freeze! after the last registration and before starting the app."},{"title":"Query Parameters","page":"Examples","location":"examples.html#Query-Parameters","category":"section","text":"Use the query() helper for type-safe access with automatic parsing:\n\nusing Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/search\", req -> begin\n    q     = query(req, \"q\", \"\")       # String default\n    page  = query(req, \"page\", 1)     # Auto-parsed to Int\n    limit = query(req, \"limit\", 10)   # Auto-parsed to Int\n    active = query(req, \"active\", true)  # Auto-parsed to Bool\n\n    json(Dict(\"query\" => q, \"page\" => page, \"limit\" => limit, \"active\" => active))\nend)\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)"},{"title":"JSON Request and Response","page":"Examples","location":"examples.html#JSON-Request-and-Response","category":"section","text":"JSON is built-in via JSON — no setup required:\n\nusing Mongoose\n\nrouter = Router()\n\n# Return JSON responses using json() helper\nroute!(router, :get, \"/users/:id::Int\", (req, id) ->\n    json(Dict(\"id\" => id, \"name\" => \"User $id\", \"active\" => true))\n)\n\n# Parse JSON request body with json()\nroute!(router, :post, \"/users\", req -> begin\n    data = parsejson(req)  # returns Dict/Array from JSON\n    json(Dict(\"created\" => data[\"name\"]); status=201)\nend)\n\n# Typed request validation into a struct\nstruct CreateUser\n    name::String\n    email::String\n    age::Int\nend\n\nroute!(router, :post, \"/users/typed\", req -> begin\n    user = validate(req, CreateUser)  # parses + validates; throws ValidationError\n    json(Dict(\"name\" => user.name, \"email\" => user.email))\nend)\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)"},{"title":"Multipart File Upload","page":"Examples","location":"examples.html#Multipart-File-Upload","category":"section","text":"using Mongoose\n\nrouter = Router()\n\nroute!(router, :post, \"/upload\", req -> begin\n    parts = parsemultipart(req)  # Dict{String, Union{String, MultipartFile}}\n    isempty(parts) && return json(Dict(\"error\" => \"No files\"); status=400)\n\n    for (name, value) in parts\n        # value is a String (form field) or MultipartFile (uploaded file)\n    end\n\n    file = parts[\"avatar\"]::MultipartFile\n    json(Dict(\"files\" => [file.filename], \"size\" => length(file.data)); status=201)\nend)\n\napp = App(; router=router, workers=4, max_body_bytes=10_000_000)  # 10MB limit\nstart!(app; port=8080)"},{"title":"Middleware Stack","page":"Examples","location":"examples.html#Middleware-Stack","category":"section","text":"using Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> json(Dict(\"status\" => \"ok\")))\nroute!(router, :get, \"/api/data\", req -> json(Dict(\"data\" => [1,2,3])))\n\napp = App(; router=router, workers=4)\n\n# Middleware runs in registration order\nuse!(app, security())                                        # Security headers\nuse!(app, health())                                          # /healthz, /readyz, /livez\nuse!(app, metrics())                                         # GET /metrics\nuse!(app, cors(origins=\"*\"))                                 # CORS headers\nuse!(app, compress(min_size_bytes=1024))                           # GZip compression\nuse!(app, logger())                                          # Access logs\nuse!(app, ratelimit(max_requests=100, window_seconds=60))    # Rate limiting\nuse!(app, bearer(t -> t == \"secret\"); paths=[\"/api\"])        # Auth on /api only\n\nstart!(app; port=8080)"},{"title":"Custom Middleware","page":"Examples","location":"examples.html#Custom-Middleware","category":"section","text":"using Mongoose\n\nstruct RequestTimer <: Mongoose.AbstractMiddleware end\n\nfunction (::RequestTimer)(req::Request, next::Function)\n    t = time()\n    res = next()\n    elapsed = round((time() - t) * 1000, digits=1)\n    @info \"$(req.method) $(req.uri)\" status=res.status ms=elapsed\n    return res\nend\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> text(\"hello\"))\n\napp = App(; router=router, workers=4)\nuse!(app, RequestTimer())\nstart!(app; port=8080)"},{"title":"Route Groups","page":"Examples","location":"examples.html#Route-Groups","category":"section","text":"using Mongoose\n\nrouter = Router()\n\n# Public routes\nroute!(router, :get, \"/\", req -> json(Dict(\"msg\" => \"welcome\")))\n\n# API group with scoped middleware\napi = group(\"/api/v1\", middleware=[\n    ratelimit(max_requests=100, window_seconds=60),\n    apikey(header_name=\"x-api-key\", keys=Set([\"my-key\"])),\n])\n\nroute!(api, :get, \"/users\", req -> json(Dict(\"users\" => [])))\nroute!(api, :post, \"/users\", req -> begin\n    data = parsejson(req)\n    json(Dict(\"created\" => data[\"name\"]); status=201)\nend)\nroute!(api, :get, \"/users/:id::Int\", (req, id) -> json(Dict(\"id\" => id)))\n\n# Nested admin group\ngroup!(api, \"/admin\", middleware=[bearer(t -> t == \"admin-secret\")]) do admin\n    route!(admin, :delete, \"/users/:id::Int\", (req, id) ->\n        json(Dict(\"deleted\" => id))\n    )\nend\n\nmount!(router, api)\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)"},{"title":"WebSocket","page":"Examples","location":"examples.html#WebSocket","category":"section","text":"using Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/\", req -> html(\"\"\"\n    <script>\n    const ws = new WebSocket(\"ws://localhost:8080/ws\");\n    ws.onmessage = e => console.log(e.data);\n    ws.on_open = () => ws.send(\"hello\");\n    </script>\n    <p>Check console</p>\n\"\"\"))\n\nws!(router, \"/ws\";\n    on_open = req -> begin\n        auth = get(req.headers, \"authorization\", nothing)\n        # Return false to reject with 403\n        @info \"WS connected\" uri=req.uri\n        true\n    end,\n    on_message = msg -> Message(\"Echo: $(msg.data)\"),\n    on_close = () -> @info \"WS disconnected\"\n)\n\napp = App(; router=router, workers=4, ws_idle_timeout_ms=60_000)\nstart!(app; port=8080)"},{"title":"WebSocket Server Push (broadcastws)","page":"Examples","location":"examples.html#WebSocket-Server-Push-(broadcastws)","category":"section","text":"WebSockets can also be pushed to: send a frame to every open client of a path from any task (background housekeeping, event relays, request handlers). Frames route through the same thread-safe reply queue the worker pool uses and are actually sent on the poll thread, so no C connection is ever touched from another thread.\n\nusing Mongoose\nimport JSON\n\nrouter = Router()\n\n# Clients subscribe to stock updates\nws!(router, \"/stock\";\n    on_message = msg -> Message(\"pong: $(msg.data)\"),\n)\n\napp = App(; router=router, workers=4)\n\n# Broadcast a stock event to everyone currently connected to /stock\nbroadcastws(app, \"/stock\", JSON.json(Dict(\"event\" => \"low\", \"sku\" => \"SHOP-MUG-6\")))\n\nstart!(app; port=8080)\n\nRequires an async executor (workers > 0); stale/closed connections are dropped silently."},{"title":"Server-Sent Events (SSE)","page":"Examples","location":"examples.html#Server-Sent-Events-(SSE)","category":"section","text":"using Mongoose\n\nrouter = Router()\n\nroute!(router, :get, \"/events\", req ->\n    sse(req) do writer\n        for i in 1:10\n            emit(writer; data=\"Tick $i\", event=\"heartbeat\", id=string(i))\n            sleep(1)\n        end\n        emit(writer; data=\"done\", event=\"close\")\n    end\n)\n\nroute!(router, :get, \"/\", req -> html(\"\"\"\n    <script>\n    const es = new EventSource(\"/events\");\n    es.addEventListener(\"heartbeat\", e => console.log(e.data));\n    es.addEventListener(\"close\", e => { console.log(\"done\"); es.close(); });\n    </script>\n    <p>Check console for SSE events</p>\n\"\"\"))\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)"},{"title":"Static File Serving","page":"Examples","location":"examples.html#Static-File-Serving","category":"section","text":"using Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> redirect(\"/static/index.html\"))\n\napp = App(; router=router, workers=4)\n\n# Serve files from \"public/\" directory at /static/* prefix\n# Supports Range requests, ETag, and gzip (handled at C level)\nserve!(app, \"public\"; uri_prefix=\"/static\")\n\nstart!(app; port=8080)"},{"title":"Cookies","page":"Examples","location":"examples.html#Cookies","category":"section","text":"using Mongoose\n\nrouter = Router()\n\nroute!(router, :post, \"/login\", req -> begin\n    c = Cookie(\"session\", \"abc123\";\n        httponly = true,\n        secure   = true,\n        max_age  = 3600,\n        samesite = :strict,\n    )\n    setcookie(c)  # serialize to a Set-Cookie header value; add it as a response header:\n    json(Dict(\"logged_in\" => true); headers=[\"Set-Cookie\" => setcookie(c)])\nend)\n\nroute!(router, :get, \"/profile\", req -> begin\n    jar = parsecookies(req)\n    session = get(jar, \"session\", nothing)\n    session === nothing && return json(Dict(\"error\" => \"unauthorized\"); status=401)\n    json(Dict(\"session\" => session))\nend)\n\napp = App(; router=router, workers=4)\nstart!(app; port=8080)"},{"title":"Custom Error Responses","page":"Examples","location":"examples.html#Custom-Error-Responses","category":"section","text":"using Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> json(Dict(\"ok\" => true)))\n\napp = App(; router=router, workers=4)\n\n# Static error responses\nonerror!(app, 500, json(Dict(\"error\" => \"Internal Server Error\"); status=500))\nonerror!(app, 413, json(Dict(\"error\" => \"Payload too large\"); status=413))\n\n# Dynamic error handler (handler receives the request)\nonerror!(app, 404) do req\n    json(Dict(\"error\" => \"Not found\", \"path\" => req.uri); status=404)\nend\n\nstart!(app; port=8080)"},{"title":"Dependency Injection","page":"Examples","location":"examples.html#Dependency-Injection","category":"section","text":"using Mongoose\n\n# Example: share a database connection\nstruct FakeDB\n    users::Dict{Int,String}\nend\n\nrouter = Router()\n\nroute!(router, :get, \"/users/:id::Int\", (req, id) -> begin\n    db = withservices(req) do svcs\n        svcs.db                       # concrete type → type-stable\n    end\n    name = get(db.users, id, nothing)\n    name === nothing && return json(Dict(\"error\" => \"not found\"); status=404)\n    json(Dict(\"id\" => id, \"name\" => name))\nend)\n\napp = App(; router=router, workers=4)\nservice!(app, :db, FakeDB(Dict(1 => \"Alice\", 2 => \"Bob\")))\n\nstart!(app; port=8080)"},{"title":"Background Tasks & Lifecycle Hooks","page":"Examples","location":"examples.html#Background-Tasks-and-Lifecycle-Hooks","category":"section","text":"using Mongoose\n\nrouter = Router()\nroute!(router, :get, \"/\", req -> text(\"running\"))\n\napp = App(; router=router, workers=4)\n\nonstart!(app) do\n    @info \"Server started, seeding data...\"\nend\n\nonstop!(app) do\n    @info \"Graceful shutdown complete\"\nend\n\nbackground!(app) do\n    while true\n        @info \"Background tick\" time=time()\n        sleep(30)\n    end\nend\n\nstart!(app; port=8080)"},{"title":"GZip Compression","page":"Examples","location":"examples.html#GZip-Compression","category":"section","text":"using Mongoose\n\nrouter = Router()\n\n# Large response that benefits from compression\nroute!(router, :get, \"/data\", req -> begin\n    large_data = Dict(\"items\" => [Dict(\"id\" => i, \"value\" => \"x\"^100) for i in 1:100])\n    json(large_data)\nend)\n\napp = App(; router=router, workers=4)\n\n# Compress responses larger than 1KB when client accepts gzip\nuse!(app, compress(min_size_bytes=1024))\n\nstart!(app; port=8080)"},{"title":"Testing with FakeTransport","page":"Examples","location":"examples.html#Testing-with-FakeTransport","category":"section","text":"FakeTransport is the FFI-free reference transport: it dispatches requests through the full pipeline with no server and no Mongoose_jll, so tests never bind a port.\n\nusing Test, Mongoose\n\n# Setup\nrouter = Router()\nroute!(router, :get, \"/hello\", req -> json(Dict(\"msg\" => \"hi\")))\nroute!(router, :post, \"/echo\", req -> begin\n    data = parsejson(req)\n    json(data; status=201)\nend)\n\napp = App(; router=router)\nuse!(app, cors())\n\nclient = FakeTransport(app)   # or: FakeTransport(app)\n\n# Test GET\nresp = client(:get, \"/hello\")\n@test resp.status == 200\n@test contains(resp.body, \"\\\"msg\\\"\")\n\n# Test POST with JSON body\nresp = client(:post, \"/echo\";\n    body = \"\"\"{\"name\": \"Alice\"}\"\"\",\n    headers = [\"Content-Type\" => \"application/json\"],\n)\n@test resp.status == 201\n@test contains(resp.body, \"Alice\")\n\n# Test with query parameters\nresp = client(:get, \"/hello\"; query=Dict(\"foo\" => \"bar\"))\n@test resp.status == 200\n\nFor deterministic async tests, FakeExecutor mirrors the AsyncExecutor contract (queued submit!, haspending, start!/stop!) but never spawns workers: jobs run only when the test calls run!, inline in submission order — no threads, no sleeps.\n\nfe = FakeExecutor()\n@test submit!(fe, () -> \"first\") == true   # enqueued, not run\n@test haspending(fe)\n@test run!(fe) == [\"first\"]                # FIFO, inline\n@test !haspending(fe)"},{"title":"Production Configuration","page":"Examples","location":"examples.html#Production-Configuration","category":"section","text":"using Mongoose\n\nrouter = Router()\n# ... define routes ...\n\napp = App(;\n    router          = router,\n    workers         = parse(Int, get(ENV, \"WORKERS\", \"4\")),\n    queue_size       = 2048,\n    max_body_bytes        = 4_000_000,       # 4MB\n    request_timeout_ms = 30_000,          # 30s\n    drain_timeout_ms   = 10_000,          # 10s graceful shutdown\n    ws_idle_timeout_ms = 120_000,         # 2min WS idle\n    header_timeout_ms  = 10_000,          # close conns that stall before a request\n    max_connections    = 10_000,          # refuse beyond this many open conns\n)\n\n# Full middleware stack\nuse!(app, security())\nuse!(app, health(ready_check = () -> true))\nuse!(app, metrics())\nuse!(app, cors(origins=get(ENV, \"CORS_ORIGINS\", \"*\")))\nuse!(app, compress(min_size_bytes=1024))\nuse!(app, logger())\n\n# Error responses\nonerror!(app, 500, json(Dict(\"error\" => \"Internal error\"); status=500))\nonerror!(app, 413, json(Dict(\"error\" => \"Too large\"); status=413))\nonerror!(app, 503, json(Dict(\"error\" => \"Overloaded\"); status=503))\n\n# Services\nservice!(app, :env, get(ENV, \"APP_ENV\", \"production\"))\n\n# Static assets\nserve!(app, \"public\"; uri_prefix=\"/static\")\n\nstart!(app; host=\"0.0.0.0\", port=parse(Int, get(ENV, \"PORT\", \"8080\")))"},{"title":"Mongoose.jl","page":"Introduction","location":"index.html#Mongoose.jl","category":"section","text":"Mongoose.jl is a production-ready HTTP & WebSocket framework for Julia, built on the Mongoose C library."},{"title":"Key Features","page":"Introduction","location":"index.html#Key-Features","category":"section","text":"Modular, pluggable core — Router, Executor, and Transport are replaceable components behind small protocols (AbstractRouter, AbstractExecutor, AbstractTransport)\nUnified App type — sync (SyncExecutor) or async worker pool (AsyncExecutor)\nBuilt-in JSON via JSON — parsejson(req) for parsing, json(...) for responses\nTyped routing — exact Dict lookup + ordered parametric patterns, typed path parameters (:id::Int) as typed tuples, wildcards, route groups; freeze! compiles a closed route table into statically-typed dispatch (the foundation for AOT builds; juliac --trim support is still in progress)\nFull middleware stack — CORS, rate limiting, bearer/API-key/basic auth, access logs, Prometheus metrics (counters, histogram, live gauges), health checks, security headers, gzip, ETag\nReal-time — WebSocket (origin allowlist, idle timeout, server push via broadcastws) and Server-Sent Events (sse / emit) on the same port\nNative TLS — HTTPS via TLSConfig\nProduction-ready — graceful shutdown (SIGINT; SIGTERM/exit via atexit), backpressure, request/header timeouts, connection caps, custom + typed errors, dependency injection, background tasks\nTestable without FFI — FakeTransport runs the whole pipeline without Mongoose_jll"},{"title":"Installation","page":"Introduction","location":"index.html#Installation","category":"section","text":"] add Mongoose"},{"title":"Quick Start","page":"Introduction","location":"index.html#Quick-Start","category":"section","text":"using Mongoose\n\napp = App(workers=4)\n\nget!(app, \"/\") do req\n    text(\"Hello, World!\")\nend\n\nget!(app, \"/users/:id::Int\") do req, id\n    json(Dict(\"id\" => id, \"name\" => \"User $id\"))\nend\n\npost!(app, \"/users\") do req\n    data = parsejson(req)\n    json(Dict(\"created\" => data[\"name\"]); status=201)\nend\n\nstart!(app; port=8080)"},{"title":"Architecture","page":"Introduction","location":"index.html#Architecture","category":"section","text":"Mongoose.jl is layered so each boundary is a replacement point:\n\n┌──────────────────────────────────────────┐\n│  App (config, lifecycle, services)       │\n│    + Router   <: AbstractRouter          │\n│    + Executor <: AbstractExecutor        │\n├──────────────────────────────────────────┤\n│  Pipeline (process) & Middleware         │\n├──────────────────────────────────────────┤\n│  Kernel (protocol, router, types)        │\n├──────────────────────────────────────────┤\n│  Transport (<: AbstractTransport)        │\n│    - C transport (Mongoose FFI)          │\n│    - FakeTransport (no FFI, for tests)   │\n└──────────────────────────────────────────┘\n\nRouter registers Endpoints (handler + scoped middleware + metadata) and resolves matches; it never runs handlers. After freeze! it compiles the closed table: per-route terminals are pre-baked (handler + scoped middleware fused, handler type captured) and parametric matching runs through a statically-typed chain with no per-request path split.\nApp composes a router, an executor, middleware, and lifecycle.\nSyncExecutor runs jobs inline; AsyncExecutor is a bounded worker pool.\nThe request→response seam (process) lives in Kernel and works with no server and no FFI."},{"title":"Units","page":"Introduction","location":"index.html#Units","category":"section","text":"Quantities carry their unit in the name: _ms for timeouts (request_timeout_ms, logger(threshold_ms=…)), _seconds for protocol durations (window_seconds, cors(max_age_seconds=…)), _bytes for sizes (max_body_bytes, compress(min_size_bytes=…)). Standard protocol fields keep their spec names (Cookie(; max_age=…) is Max-Age in seconds). Optional headers/values are turned off with nothing."},{"title":"Next Steps","page":"Introduction","location":"index.html#Next-Steps","category":"section","text":"Examples — cookbook-style recipes for common patterns\nPerformance & Deployment — measured baselines and the production recipe\nAPI Reference — full documentation of all exported types and functions"}]
}
