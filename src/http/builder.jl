"""
    HTTP request builder — constructs typed request objects from C event data.
"""

# Request/ViewRequest are constructed directly via their struct constructors.
# See http/types.jl for Request(::MgHttpMessage) and ViewRequest(::MgHttpMessage).
